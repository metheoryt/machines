# dream run — 2026-09-11

**Box:** `g15` (`g513ie`, Ubuntu 26.04) · **unattended** · repo clean on `main` at start ·
`git pull --ff-only` → already up to date.

**This is the FIRST run.** `docs/dream/` held only `README.md` — no `queue.md`, no
`ledger.tsv`, no `runs/`. So nothing was suppressed, `verify` had nothing to check, and
every number below is a baseline rather than a delta.

**Invariant held.** No memory store, `CLAUDE.md` or `kb-harvest-state.json` was edited.
Both Step 0/Step 8 `git status --porcelain` snapshots (repo-side `.claude/memory/project.md`;
dotfiles-side `~/.claude/memory` + `~/.claude/host-memory.md`) were empty before and after.

---

## Corpus scanned

**14 memory stores, 583 KB.** Two stores hold **70%**:

| store | bytes | scope |
|---|---|---|
| `machines/.claude/memory/project.md` | 283052 | repo:machines |
| `~/.claude/memory/global.md` | 123859 | shared |
| `my/skep/.claude/memory/project.md` | 53021 | repo:skep |
| `my/vps/.claude/memory/project.md` | 42573 | repo:vps |
| `~/.claude/memory/personality/practices.md` | 22141 | shared |
| `my/qaz-code/.claude/memory/project.md` | 22383 | repo:qaz-code |
| `my/embedthat/.claude/memory/project.md` | 15870 | repo:embedthat |
| `~/.claude/memory/personality/tone.md` | 9116 | shared |
| `~/.claude/host-memory.md` | 6121 | host |
| `~/.claude/memory/personality/values.md` | 6068 | shared |
| `~/.claude/memory/personality/habits.md` | 5821 | shared |
| `~/.claude/memory/core.md` | 3004 | shared |
| `my/airdrome/.claude/memory/project.md` | 2932 | repo:airdrome |
| `my/telegrind/.claude/memory/project.md` | 1880 | repo:telegrind |

**15 auto-loaded instruction files, 213 KB** — materially larger than the skill's stated
~116 KB over 9 files, because **this box holds the `my` repo group and no other box does**.
`my/qaz-code/CLAUDE.md` at **59558 B** is the largest instruction file in the corpus,
bigger than `machines/AGENTS.md` (46572 B).

Nothing was unreadable. Nothing scanned as `untracked`.

---

## Filed this run

**103 items**, 213 KB of queue.

| action | n | | action | n |
|---|---|---|---|---|
| contradiction | 29 | | generalise | 8 |
| dedupe | 19 | | delete | 8 |
| compress | 16 | | promote | 6 |
| demote | 12 | | skill | 3 |
| | | | untracked · harvest | 1 · 1 |

**Suppressed: 1.** One subagent independently re-derived the gortex-import contradiction
against `~/.claude/CLAUDE.md` that this run had already filed, and `append` correctly
dropped it as `open` — the same `(target, anchor, action)` triple produced the same id
`68f42627` from two independent derivations. That is the identity check working.

### Bytes, now → if every open item were applied

Across the 102 items carrying a parseable byte pair: **186022 → 89426, a saving of
96596 B.**

**Read that honestly.** It is a per-store figure, not a net-corpus one: the 12 `demote`
items show `N → 0` at the source while the text moves to a destination store, so the
corpus shrinks by much less than 96 KB. What the number does measure is **how much text
stops being loaded where it is loaded today** — which is the thing that costs context
every session. The largest single reductions:

| item | store | saving |
|---|---|---|
| `f1d3be7d` | `my/embedthat/CLAUDE.md` → pointer stub | **12728** |
| `f76e1f4f` | `global.md` kb-refresh section → machines | 9377 |
| `fdaea448` | `project.md` guards-that-report-success | 5629 |
| `37c0a0ba` | `skep/project.md` build journal | 4575 |
| `8e41cba2` | `global.md` Pure logging → the work repo | 6760 |

---

## The findings that matter most

**1 · Shared memory is not shared. `main` has not moved since 2026-08-27.** (`2f658c5e`)
`~/.claude/CLAUDE.md` asserts the six shared stores are "byte-identical everywhere". By
blob hash they are **not**, on **all six**, and every live box is *ahead* of `main` —
this is divergence, not lag. Three clusters: A = `main` + the dead `g15-wsl`; B =
latitude + hub + g15; C = `desktop-wsl`, furthest ahead on every personality facet.
A box re-checking out from `main` gets a `global.md` 24.5 KB smaller than g15's and a
`practices.md` 9.8 KB smaller than desktop-wsl's; `main`'s `values.md` is missing 2.8 KB
that three live boxes carry, including non-negotiables `core.md` points at.
Per-box promotes filed as `6d32518b` (desktop-wsl, ~17.8 KB stranded), `08ade540` (air,
which is *also* the only box with `main` unmerged), `6b30ae15` (g15).

**2 · A rule that fires on this box and tells the agent to do what the hook blocks.**
(`68f42627`, plus the host-scoped `0a184245`) `~/.claude/CLAUDE.md` imports
`@/home/me/.gortex/instructions/active.md`; that path **does not exist here**, so the
import is empty, so the file's own escape hatch concludes "this box has no gortex — use
native Read/Grep/Glob". But gortex *is* installed, the daemon was up at session start, and
the deny hook blocked four greps during this very pass. `~/.claude/host-memory.md`
independently says "Gortex tracks nothing on this box (`repos: []`)" while
`~/.gortex/config.yaml` lists five tracked repos.

**3 · 22.9 KB of employer-repo content in a store every box loads every session.**
Seven `Pure …` sections — 18% of `global.md` — including colleagues' emails, Slack UIDs
and pronouns, sitting byte-identical on the VPS. Filed one item per section
(`8e41cba2` `6846453c` `9e243a8c` `c18ef4d8` `6c384b09` `d0474135` `98c3201a`), each with
a deterministic `sed` carry recipe. **All seven carry a blocker:** the destination store
`pure/backend-api/.claude/memory/project.md` is tracked on `origin/desktop-wsl` **only,
not on `main`**, so demoting into it today *reduces* availability from every box to one
branch. Promote that store first.

**4 · `machines/AGENTS.md` asserts five pairs of incompatible things.** (`398b5299`,
`93ec8377`, `e05f4c1e`) Role-with-no-executor fails vs. prints a plan; g15 is a Windows
dispatch target vs. `platform: debian`; `g15-wsl` described in the live present tense vs.
"GONE — reinstalled 2026-09-07"; `hosts/desktop/windows/` carries backup scripts vs. does
not; and g15's role list omits `backup-client` twenty lines before the same entry argues
it has it. Filed with both halves quoted and **no replacement** — the file's own rule is
that a human picks. Separately, it states the don't-write-counts-into-prose rule three
times and then carries eight live counts.

**5 · Instruction files duplicated wholesale.** `embedthat` ships `CLAUDE.md` and
`AGENTS.md` differing only in the title line and one sentence — and that sentence is a
botched find-and-replace reading *"guidance to Codex (Codex.ai/code)"*. `skep`'s pair is
byte-identical. `airdrome` already solved it with a 135-B pointer stub; both are filed to
adopt it (`f1d3be7d`, `9d35c9b2`).

**6 · Stale *instructions*, which are worse than stale facts.** Several items exist only
because a section still **orders** a future session to do something already done or now
wrong: preserve a `pgdata` copy the owner deleted (`f48622f0`); keep a
`ConditionPathIsMountPoint` that was removed precisely because it reported success for 90
minutes with the disk gone (`05d9c0ce`); free a disk only after a qBittorrent check that
passed the same day (`4b32b623`).

---

## Standing checks

- **`core.md` size — and the check itself is wrong.** File is **3004 B**, over the 3000
  "urgent" line. But the loader **strips HTML comments**, so what actually reaches the
  model is **2442 B**, well under the ~3.4 KB hook cap: `mem_warn_if_over` was never
  firing, and the ~2 KB figure is a file-byte style convention, not a truncation margin.
  Compression filed anyway (`7f7db67c`, 2442 → 1937 injected) because the budget is real;
  the mis-measurement is filed against the skill (`e9310fe2`). A second discovery in the
  same pass: `register-reinject.sh` **exists and re-injects only the first line** of each
  marked bullet, which `tone.md` still says does not exist (`1d51905a`).
- **Transcript gap: 30 of 30 unharvested on this box** (`0c1fdb6a`). `kb-harvest-state.json`
  lists 129 sessions; `~/.claude/projects/*/*.jsonl` holds 30; the intersection is empty.
  **kb-refresh was NOT run** — `fleet-gather.sh` advances its watermark at gather time,
  before its own review gate, so an unattended gather would strand those 30 permanently.
- **Untracked stores: none.**
- **`verify`: nothing to verify** — no prior applied decisions.
- **Dead branch checked rather than assumed** (`aed78e2f`): `origin/g15-wsl` is
  byte-identical to `main` on all six shared stores by blob hash and carries no
  `host-memory.md`, so it holds **zero unique facts**. The live instance of "a retired
  branch can be the last copy" is `origin/desktop-wsl`'s 58 KB `pure/backend-api` store.
- **`origin/desktop` has no personality facets tracked at all** (`6c10ae40`), and its
  `core.md`/`global.md` are mode-120000 symlinks to `//wsl.localhost/desktop-wsl/…` —
  resolvable only while that distro is running.

---

## Conventions this run invented, so the next run reproduces the same ids

`dream.sh index` emits `##` rows only, and several findings have no heading at all. Until
the skill settles this (filed as `f7082e6e`), this run used:

- whole-file finding → anchor **`(whole file)`**
- per-branch finding → anchor **`(branch: <name>)`**
- fleet-wide finding about the shared set → anchor **`(fleet: shared stores vs origin/main)`**
- a finding under a `###` → the **enclosing `##`**, with the `###` named in the `why`

And where one `(target, anchor, action)` triple carried several distinct findings, they
were merged into **one item with numbered `### Part N` sub-decisions**, each with its own
evidence, bytes and replacement. That is a workaround for an id collision, not a design —
see `f7082e6e`.

## Skill items filed against `/dream` itself

- **`f7082e6e`** — the id triple cannot express two findings of one action in one section
  (`Fleet network` alone produced 3+3), and `index` cannot name `###` headings or
  whole-file findings. Proposes a four-tuple id including the evidence line range.
- **`35ce7179`** — Step 3b cites `origin/server`, **which no longer exists**, and its
  dead-branch warning is false for the only dead branch there is. Proposes citing the
  check rather than the expected result.
- **`e9310fe2`** — Step 5's `core.md` budget measures file bytes against a stdout cap that
  applies to injected bytes; the two differ by 562 B here, which is what made this run
  report "urgent".

## Notes for whoever works the queue

- **`/improve config audit` overlaps.** This run filed items against `machines/AGENTS.md`,
  `~/.claude/CLAUDE.md`, `~/CLAUDE.md`, `my/qaz-code/CLAUDE.md`, `my/embedthat/CLAUDE.md`
  and `my/skep/CLAUDE.md`. Don't let both skills propose against those the same day.
- **Two items disagree on one section, on purpose.** `b43527e5` (delete) and a compress
  proposal from a second agent both target `qaz-code/.claude/memory/project.md`
  *Corpus state and the backfill*; they disagree about whether `CLAUDE.md` already carries
  the three facts. `b43527e5` records the conflict inline. Resolve it before applying.
- **A better replacement exists for `68f42627` than the one in the item.** That item was
  filed early as "a human picks between two fixes"; a later subagent produced concrete
  text. The queue is append-only from a run's side, so it was not edited in. Use this:

  > **Do not read an empty import as "no gortex" — that test does not work.**
  > Measured on g15 2026-09-11: `~/.gortex/instructions/` does not exist, so the
  > import above resolves to nothing, while gortex IS installed
  > (`~/.local/bin/gortex`, MCP live, `machines` indexed) and serves its rules
  > through the MCP hook's per-call context rather than that file. **The test that
  > works is whether the `mcp__gortex__*` tools are callable in this session.** If
  > they are, the mandate above applies whatever the import did.
  >
  > Only if they are NOT callable does this box lack gortex — the `server` and `hub`
  > provisioning profiles omit it deliberately (see the profile comments in
  > `machines/provision/linux.sh`), because a code-graph daemon wants indexed source
  > checkouts and a services box or a 960 MB VPS has none. There, use the native
  > Read/Grep/Glob tools. Never conclude that from the import: where gortex IS
  > installed the `--hook-mode deny` posture blocks `Read`/`Grep`/`Glob` on indexed
  > source (below), so acting on the old test produced blocked calls, not a fallback.

  Place it **outside** the `<!-- gortex:rules:end -->` marker so `gortex install` cannot
  clobber it. The `server`/`hub` carve-out was verified still true against
  `provision/linux.sh`:117,134.

## Run conditions worth recording

- The **gortex daemon went unreachable partway through** and enforcement stood down to
  rule-only. Reading was done with the shell throughout, per the skill, so nothing was
  blocked — but it is a second way this box can look like "no gortex" while gortex is
  installed, which is the subject of `68f42627`.
- **This box is the only one that can see six of these stores.** `fleet.json` gives g15
  `repo_groups: ["my"]`; `skep`, `vps`, `qaz-code`, `embedthat`, `airdrome` and
  `telegrind` are checked out nowhere else. 41 of the 103 items are against those stores
  and their instruction files.

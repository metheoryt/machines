# machines — full path ledger (240/240)

Every tracked path at `repo-review-cleanup`, one row each, sorted most-actionable first.
Companion to `2026-08-03-repo-review.md`. Produced 2026-08-03.

**Tally:** delete 6, needs-decision 11, merge 2, rewrite 86, keep 135

| Path | Verdict | Reason | Referenced by |
|---|---|---|---|
| `.superpowers/sdd/progress.md` | delete | A finished SDD scratch ledger — both features read "STATUS: COMPLETE on main" (lines 16, 48) and its two Plan: pointers are dead /tmp/claude-1000/-home-me-... Linux scratchpaths; its one line of still-live content (line 49, "re-paste the two one-liners into Orca ... bash \"$HOME/machines/agents/worktree-teardown.sh\"") is now actively WRONG, superseded by README.md:156-164 which says /orca-setup prints `wt-setup`/`wt-teardown`, reports the long form as LEGACY not a conflict, and states "nothing needs re-pasting." | nothing found (only a passing mention in docs/superpowers/plans/2026-07-28-latitude-wipe-harvest.md:423 listing it as a dirty worktree file) |
| `docs/superpowers/handoffs/2026-07-17-fleet-ssh-over-tailnet-handoff.md` | delete | A handoff addressed "For: the NEXT session" whose next session ran (roadmap:505, AWG retired 2026-07-17) and both of whose "Immediate cleanup still pending" items are shipped — provision/windows.ps1:270 now sets the rule to RemoteAddress 100.64.0.0/10 and :275 disables OpenSSH-Server-In-TCP, exactly the fix it asks for; its entire evidence base is deleted code (modules/system/mesh-vpn.nix, modules/home/ssh.nix, provision/mesh-authorized-keys, latitude as NixOS at 100.64.0.2 — now Debian at .8) and it names Opus 4.8 commit trailers and `just switch`; its one durable ask, the ssh-server firewall shape, now lives in docs/2026-08-01-nixos-harvest.md §1, which roadmap P3 and AGENTS.md both point at. Recoverable from c3ecf69 and tag nixos-final. | nothing found |
| `docs/superpowers/specs/2026-07-08-pre-commit-hooks-design.md` | delete | Self-declared "reverted (2026-07-18)" at line 4, its engine git-hooks.nix went with the flake in f3d63b2, and the manual gate its revert note points at (`just fmt` / `just check`) does not exist — the justfile has no fmt, check, statix, alejandra, deadnix or shellcheck recipe. | nothing found |
| `docs/superpowers/specs/2026-07-09-gortex-align-type-agnostic-design.md` | delete | The artifact it redesigns left this repo by deliberate policy — commit 1b0414c ("move the three untested skills to dotfiles": a skill with tests/ is fleet tooling and stays, one without is $HOME content) deleted agents/plugin/skills/gortex-align/, which now lives dotfiles-tracked at ~/.claude/skills/gortex-align. | nothing found |
| `docs/superpowers/specs/2026-07-15-orca-serve-wsl-design.md` | delete | Header says "implemented" but the implementation was removed — f95cb3f deleted provision/orca-serve.sh ("Orca runs on Windows now"), tore the unit down live on desktop/Ubuntu-26.04, and that same commit deliberately preserved the one durable fact (per-distro tailnet node identity) into memory, tailscale-wsl.sh and provision/README.md. | docs/superpowers/plans/2026-07-15-orca-serve-wsl.md:454 |
| `statix.toml` | delete | statix is a Nix linter and there are ZERO .nix files left in the repo; both invocation paths it names in line 1 are gone — no `check` recipe exists in the justfile, and agents/git-hooks/ holds only post-checkout, post-merge, post-rewrite and _refresh-claude-config with no pre-commit hook anywhere. `git log` shows it arrived in 2b23875 "add pre-commit hooks via git-hooks.nix", and project.md records that mechanism being removed in 2af7c5b. | nothing found (only historical mentions in docs/superpowers/specs/2026-07-08-pre-commit-hooks-design.md and two plan docs) |
| `.gemini/settings.json` | needs-decision | 42 lines wiring gortex hooks + MCP for the Gemini CLI, but `grep -rn gemini provision/ agents/bootstrap.sh` returns ZERO hits — no tier or bootstrap step installs the gemini CLI on any box, and the only in-repo mentions are a 2026-06-30 design doc noting "A `.gemini/` dir exists (project settings.json only)" and a Windows-backup exclusion list; whether the user still drives Gemini here is their call, not mine. | nothing found (no in-repo reader; the Gemini CLI itself would read it, but nothing in the repo provisions that CLI) |
| `agents/plugin/commands/.gitkeep` | needs-decision | 0 bytes, and `git log --all --diff-filter=A -- agents/plugin/commands/*` returns nothing — the dir has never held a command file, only this placeholder carried through the 1e355f3 restructure; but agents/README.md:51 documents `commands/` as part of the plugin's structural contract, so dropping it is a two-file change and the user owns whether to keep the extension point. | agents/README.md:51,187 (documented, never populated) |
| `agents/plugin/skills/kb-refresh/tests/test_distill.py` | needs-decision | 11 real test functions covering watermarking, malformed-line survival and state merge, but they are pytest-native (`tmp_path` fixture at lines 61, 96, 147...) and `python3 -c "import pytest"` fails on air with ModuleNotFoundError; nothing in any .sh, Justfile or doc invokes them. Two ways out — wrap it importlib-style the way orca-repair.test.sh does (no pytest needed), or add pytest to the toolchain — and that is the user's call. | nothing found |
| `docs/superpowers/plans/2026-07-05-machines-fleet-layout-B-backup.md` | needs-decision | ABANDONED and since reversed by decision — `git log --diff-filter=A -- 'backup/*'` is empty, so `machines/backup/` never existed; AGENTS.md:52 now settles the boundary the opposite way ("vps owns … the restic profiles"); it names g16, `homeserver` and `~/gh/vps`, all dead. Deletion is the factual verdict, but `git log --diff-filter=D -- 'docs/superpowers/plans/*'` is empty — no plan has EVER been deleted from this repo — so being the first is the user's call, not mine. | nothing found |
| `docs/superpowers/plans/2026-07-28-home-config-generator-collapse.md` | needs-decision | Its stated blocker EXPIRED rather than was met, and the fleet then moved the opposite way — §4 blocks on "modules/home/*.nix loses its only consumer when latitude is wiped", which happened (`modules/` is gone, f3d63b2), so §5 step 6 is done by accident; but step 5 is not (`tier_git_base`:662, `tier_ssh_accounts`:924, `tier_shell_init`:1092 all still render), step 2's capture window closed with the wipe, and docs/2026-08-01-nixos-harvest.md:132-133 records me.nix's git/fish/starship config being rehomed *into* `tiers.sh` — i.e. into more rendering, the inverse of this plan's thesis. Whether the collapse is still wanted is the user's call. | nothing found |
| `docs/superpowers/specs/2026-07-05-windows-restore-script-design.md` | needs-decision | Never built — no restore.ps1 anywhere in `git ls-files`, and its stated companion `hosts/g16/windows-reinstall/backup.ps1` is also gone (hosts/desktop/windows/ holds only install.ps1, the runbook and winget-packages.json); a Windows restore flow may still be wanted, so the drop is the user's call. | nothing found |
| `docs/superpowers/specs/2026-07-07-fleet-mesh-vpn-ssh-design.md` | needs-decision | Its world is retired (AmneziaWG mesh gone, mesh-vpn.nix / mesh-vpn-params.nix / rustdesk-config.nix deleted with the Nix tree, §4's mesh-authorized-keys now the self-documenting provision/fleet-authorized-keys, firewall shape re-recorded in docs/2026-08-01-nixos-harvest.md §1, rollout narrative in docs/fleet-mesh-history.md) — but §4's "pin, don't blind-TOFU" host-key rationale (preserve ssh_host_ed25519_key across reinstalls) is written nowhere else and still applies to the unimplemented P3 ssh-server role. | docs/superpowers/plans/2026-07-07-fleet-mesh-vpn-ssh.md:13; specs/2026-07-08-fleet-provisioner-phase5-mesh-executor-design.md:8; specs/2026-07-17-fleet-ssh-tailnet-retire-awg-design.md:4 |
| `hosts/desktop/windows/install.ps1` | needs-decision | Provably broken: line 66 hands off to `hosts\g16\windows\restore.ps1` — dead twice over (g16→desktop rename 2026-07-20; restore.ps1 deleted 2026-07-31 in 1080828), so line 67 throws unconditionally and steps 3-4, the file's entire purpose, cannot run; 1080828 kept it as "install.ps1 (Win11 install media, unrelated)" which misidentifies it (it is the restore bootstrap, not install media), so the recorded intent-to-keep was formed about a different file — and its fate is inseparable from the runbook's, which roadmap line 487 already lists as an open decision. | hosts/desktop/windows/windows-reinstall-runbook.md:150 (the `irm .../install.ps1 \| iex` Phase 4.0 entry point); its own line 7 publishes a public raw.githubusercontent URL; no code invokes it |
| `hosts/desktop/windows/windows-reinstall-runbook.md` | needs-decision | docs/fleet-roadmap.md:487 already logs "decide `windows-reinstall-runbook.md`'s fate" as open — and the decision is now overdue: line 43 says "All of Phase 1 is automated by backup.ps1" and line 152's entry point runs restore.ps1, both deleted in 1080828; line 131 links `[./autounattend.xml](./autounattend.xml)` but the file lives at install-media/ (the 2026-07-05 plan's Task 3 Step 4 repoint was never done); lines 145-146 and 157 route through the deleted NixOS tree (`modules\system\git-autofetch\git-autofetch.ps1`, `modules/home/claude.nix`, `just switch`); line 9 still calls the machine `g16`; and the reinstall it documents already happened (line 7 notes the post-2026-07 letter change). | provision/windows.ps1:59 uses its FILENAME as a live backup-root sentinel (`Test-Path <L>:\backup\windows-reinstall-runbook.md`) — that tests for the SSD copy, so removing the repo file would not break it; also README.md:112-ish and install-media/README.md:33 |
| `provision/provision.ps1` | needs-decision | Nothing invokes it: windows.ps1 (the documented Windows entry point) never mentions it, justfile:80 runs provision.sh, and no README/AGENTS line gives its invocation — yet it is the ONLY path that can execute roles/*.ps1, because provision.sh under Git Bash resolves platform=windows and every role_* falls to the 'no posix executor (skipped)' arm. Its $RoleExecutors hashtable (18-22) is also hardcoded where provision.sh auto-discovers via `declare -F` (11-15,59-60), so a future role silently no-ops on Windows. Wire it in or retire the PS role branch — the user's call. | nothing found outside docs/fleet-mesh-history.md and the roles/*.ps1 header comments |
| `provision/tests/roles.test.sh` | needs-decision | Correct today and green, but lines 35 and 40-53 assert the `nixos` platform arms that still exist in provision/roles/repos.sh:21, agents.sh:20 and dotfiles.sh:116 — unlike the two sanctioned leftovers (bootstrap.sh's /etc/NIXOS skip, converge.sh's nixos class, which both fail SAFE) these arms would actively provision a platform with no host, so whether they stay is a call on roles/*.sh that this file merely mirrors. | just test — justfile glob `provision/tests/*.test.sh`; subjects are provision/roles/{agents,dotfiles,repos}.sh |
| `docs/superpowers/specs/2026-07-08-fleet-provisioner-phase5-mesh-executor-design.md` | merge &rarr; `docs/fleet-mesh-history.md` | Designs mesh-member/mesh-hub executors for the AmneziaWG mesh retired 2026-07-17 — provision/roles/ holds only agents/dotfiles/repos, there is no provision/lib/mesh.sh, and 2026-07-17-fleet-ssh-tailnet-retire-awg-design §B explicitly deletes every file this spec creates; docs/fleet-mesh-history.md:281-300 already absorbs the outcome (5a executed f9843bc..73d8276, 5b never built, the VPS-is-key-authority decision), so merging resolves that doc's dangling path citation in one move. | docs/fleet-mesh-history.md:281; plans/…-phase5a-nix-single-source.md:190; plans/…-phase5b-mesh-executors.md:378 |
| `docs/superpowers/specs/2026-07-13-headscale-fleet-mesh-probe-design.md` | merge &rarr; `docs/fleet-mesh-history.md` | A one-time validation exercise, executed — its results live in plans/2026-07-13-headscale-probe-results.md and the tailnet is production. Its Decisions box (cc.cyphy.kz, embedded DERP, SQLite, no OIDC) describes the control server, which is owned by the sibling repo: ~/my/vps/vps/setup-headscale.sh and ~/my/vps/vps/headscale/config.yaml both exist, so nothing here is the only copy; only the "Headscale for our fleet, AmneziaWG for RU relatives" split is durable. | docs/superpowers/plans/2026-07-13-headscale-probe-results.md:5 |
| `.claude/memory/project.md` | rewrite | 1664 lines / 122 KB `cat` verbatim into EVERY session by agents/plugin/hooks/project-memory-check.sh:34, and it has regrown 3.6x since kb-harvest-state.json records the 2026-07-24 prune to 456 lines; content has gone stale — the last Pending follow-up still tracks "Drop pylspFixOverlay from flake.nix" and the VPS item names `mesh-hub`/`mesh-member` roles, but flake.nix does not exist and the mesh was retired 2026-07-17. | agents/plugin/hooks/project-memory-check.sh:28-35 (injected into every session); agents/worktree-setup.sh:141; provision/dotfiles-sync.sh:110 |
| `.gitattributes` | rewrite | The rule is correct and load-bearing — it is the ONLY eol=lf coverage for scripts/*.sh and hosts/latitude/debian/*.sh (agents/ and provision/ carry their own `* text eol=lf`, which is what actually covers the extensionless git-hooks) — but line 2's comment still names "nixos" as a target platform and there is no NixOS host left; comment-only fix, the rule must stay. | git checkout on the Windows box (justfile:19 + provision/windows.ps1 run these via Git Bash) |
| `.gitignore` | rewrite | Three dead Nix stanzas out of 69 lines: lines 6-8 (`result` / `result-*` build artifacts), 20-21 (`.direnv/` — project.md records that commit 2af7c5b removed the committed .envrc whose sole job was that nix-direnv GC root), and 34-35 (`nixos-hardware/` flake inputs); zero .nix files remain in the repo. The other stanzas (.claude/settings.local.json, .gortex/, provision/secrets/, /.machines/, fleet.local.json) are all accurate and verified live. | git (root ignore file) |
| `AGENTS.md` | rewrite | Self-contradicts on the repo's only gate — line 82 says "27 suites", line 132 says "28 suites", and the real count is 38 files of which `just test` reaches 28; separately the `server` entry (lines 30-45) spends 16 lines on a machine that left fleet.json 2026-08-01 and contains a mangled sentence at 39-41 ("**Reach it as `methe@server.gg.ez`, naming the user**: bare / `ssh server.gg.ez` works..."). Everything else checked out current against fleet.json and the live tree. | loaded every session via the CLAUDE.md symlink (memory discovery); agents/README.md and docs/ cite it as the entry point |
| `README.md` | rewrite | Repeats a claim that is measurably false: line 63 bills `just test` as running "every `*.test.sh` in the repo" and line 75 says "All 28 suites pass", but 38 test files exist and the recipe's four-directory glob reaches only 28. Two lesser issues: line 26 carries a punctuation artifact ("...next provision run), .") and lines 138-195 give 58 lines (30% of the file) to a worktree-dispatcher deep dive that belongs with agents/README.md. The Nix and server sections are correctly current. | repo entry point for a newcomer; no programmatic reader |
| `agents/.gitignore` | rewrite | Line 9 `settings.local.json   # machine-local per profile (gortex hooks / Sentry secret) — never commit` has an inline trailing comment, which gitignore does not strip — verified in a scratch `git init` that `foo.json   # comment` leaves `foo.json` unignored and instead ignores a file literally named `foo.json   # comment`; `git check-ignore -v agents/settings.local.json` returns NOT IGNORED (root .gitignore:39 only covers `.claude/settings.local.json`), so the one file the line calls out as secret-bearing is the one file in this list that is not actually ignored — and separately the trailing `!/memory/projects/` re-include block is dead (`agents/memory/` does not exist, `git ls-files agents/memory` is empty since the corpus moved to dotfiles 2026-07-28). | git; agents/README.md:61-76 restates the list |
| `agents/README.md` | rewrite | Three independent staleness grounds: (a) the whole `### Linux / macOS — the nix way (optional)` section at 422-436 plus refs at 163, 382-384, 417, 451-471 describes `modules/home/claude.nix`, `pkgs/gortex.nix`, `modules/programs/development.nix` and `just switch` — all deleted 2026-08-01 in f3d63b2; (b) lines 8, 49 and 149 present `settings.pure.json` → `~/.claude-pure` as a committed registry entry, but `ls agents/` shows only `settings.json` (AGENTS.md: folded back in d48c09a); (c) line 73 states `extraKnownMarketplaces` has "no absolute paths" as the reason plugins are portable, which agents/settings.json contradicts. | AGENTS.md architecture table; humans only |
| `agents/plugin/hooks/worktree-workflow.sh` | rewrite | Line 41's comment points the base-resolution order at `agents/statusline-command.sh, section 1`, but that file was deleted from this repo in 22db1bb ("now dotfiles-owned") and lives at ~/.claude/statusline-command.sh — the cross-reference a reader would follow no longer exists in the repo. Logic itself is correct and tested. | agents/plugin/hooks/hooks.json:20-23; agents/docs/git-workflow.md:48 |
| `agents/plugin/skills/kb-refresh/SKILL.md` | rewrite | Line 113 tells Track B to diff "e.g. `modules/`, `hosts/`, `provision/` in this repo" — `modules/` is the deleted NixOS tree (removed 2026-08-01 in f3d63b2), so the skill's own worked example names a directory that no longer exists. Everything else (fleet dispatch, WSL handling, tier table) matches the current fleet. | invoked as /cyphy:kb-refresh; agents/docs/kb-cron-prompt.md:52 |
| `agents/plugin/skills/orca-repair/orca-repair.py` | rewrite | orca_running()'s docstring at line 166 states "BOTH own orca-data.json, so --apply must not write while either runs" — directly contradicted by apply_should_child... by apply_should_block at lines 200-212 ("block only on the UI, not on a harmless lingering daemon") and by SKILL.md:70-71; the docstring is the pre-correction version and misleads anyone reading the guard. | agents/plugin/skills/orca-repair/SKILL.md:46,51,64-65; tests/orca-repair.test.sh:9 |
| `agents/plugin/skills/orca-setup/SKILL.md` | rewrite | Line 77 asserts the wt-setup/wt-teardown wrappers are "present and on PATH on all six boxes", but fleet.json now has 4 members (air, desktop, hub, latitude) plus the self-declared desktop-wsl = 5; the count predates server leaving the manifest. The g513ie citations at lines 169 and 186 are historical verification notes and are fine as-is. | invoked as /cyphy:orca-setup |
| `agents/settings.json` | rewrite | `extraKnownMarketplaces.pure-team.source.path` is the hardcoded absolute Linux path `/home/me/pure/claude-plugins`, which does not exist on this box (air is macOS, $HOME=/Users/me — `ls` fails on both spellings), yet this file is copy-deployed byte-identical to every profile on every platform; agents/README.md:73 states the opposite as the reason plugins are portable ("no absolute paths"). Last touched 2026-07-25, before the fleet lost its Linux dev box. Whether a dead marketplace path degrades silently or errors at launch is untested from here. | agents/bootstrap.sh:476-479 (copy_managed baseline; falls back here when settings.<postfix>.json is absent) |
| `docs/2026-07-drive-migration-log.md` | rewrite | Line 10 frames it as live operational reference ("Read this before touching any drive on latitude"), and the file's own supersession convention (~~strike~~ + "**superseded 2026-07-31**", applied to lines 19-26) was NOT applied to three later blocks that the 2026-08-01 work overturned: lines 20 and 207-219 still assert "no restic repo is planned at all", contradicted by fleet-roadmap.md:118 (repo 14f4eab544, daily 04:30, weekly check); lines 38-45 still say "latitude5520 has no dedicated backup today"; lines 194-202 still describe the navidrome/PicardedMusic plan the user has since abandoned. The measurement evidence (UUIDs, byte counts, dock serials, the -H proof) is worth every byte — only the strategy bullets need striking. | .claude/memory/project.md:692; docs/fleet-roadmap.md:503 |
| `docs/fleet-roadmap.md` | rewrite | Load-bearing live backlog, but it contradicts itself: P0's "What died" (lines 89-100) asserts in present tense "The restic repos no longer exist… latitude has the restic binary but no repo, timer, or container" and "restic-server … has been Exited (0) for 3 days", while lines 118-128 record repo 14f4eab544 built and restic-server up on 100.64.0.8:8001 — a top-down reader grepping "restic" lands on the false one; and P2's header (line 201) says "see the Forgejo item below before wiping it" when that item says wiped and the real gate is C: (line 285). Fix = past-tense/dateline the "What died" block and repoint the P2 header; the completed-narrative sections are deliberate and should stay. | README.md:28,35; AGENTS.md:33,39,141,153; .claude/memory/project.md:25,452,1608,1613; 5 docs/superpowers plans+specs |
| `docs/superpowers/plans/2026-06-30-codex-support.md` | rewrite | IMPLEMENTED (codex/hooks.json added in 99b07cb) then restructured away: the `codex/` tracked dir it designs was folded into `agents/` by 8ef2005 (2026-07-02) and its Nix home-module wiring died with the flake; Codex support itself is still live (agents/bootstrap.sh and agents/plugin/hooks/global-memory-load.sh both reference `.codex`), so it needs a status header naming what survived, not deletion. | nothing found — grep for the filename outside docs/superpowers/plans/ returns no hits |
| `docs/superpowers/plans/2026-07-02-agents-rename-per-profile-bootstrap.md` | rewrite | IMPLEMENTED — `agents/` and `agents/subagents/` exist and `just agent-bootstrap-profile <postfix>` is the live profile-aware entry point; but its wiring half names `modules/home/claude.nix`/`codex.nix` (deleted with the Nix tree in f3d63b2) and `agents/codex/` (moved to dotfiles), so 0/20 boxes plus dead paths read as un-executed work. | nothing found |
| `docs/superpowers/plans/2026-07-02-unified-profile-settings-sharing.md` | rewrite | IMPLEMENTED 2026-07-02 (84a5a84 created agents/settings.personal.json + settings.work.json) then REVERTED: d48c09a (2026-07-16) retired the second profile and only `agents/settings.json` remains, while the whole Nix half (modules/home/claude.nix, mkOutOfStoreSymlink) was deleted with the flake — needs an implemented-then-reverted header naming 2026-07-18-profile-provisioning-unification.md as the successor. | nothing found |
| `docs/superpowers/plans/2026-07-04-cyphy-plugin.md` | rewrite | IMPLEMENTED — every artefact exists: `agents/plugin/.claude-plugin/plugin.json`, `agents/plugin/hooks/hooks.json`, `agents/plugin/{agents,commands,skills}`; the pre-plugin `agents/{skills,hooks,commands}` are all gone, and the `settings.personal.json`/`settings.work.json` pair it assumed was folded back into one `settings.json` in d48c09a. Needs a status header, not work. | nothing found |
| `docs/superpowers/plans/2026-07-05-machines-fleet-layout-A-reorg.md` | rewrite | IMPLEMENTED — the layout it specifies is the tree today (hosts/<name>/<platform>/, install-media/{autounattend.xml,ventoy.json,README.md}, scripts/) — but every identifier in it is two renames stale (`g16`/`homeserver`/`latitude5520`, `~/nix`, `flake.nix`, `just quick`/`nix flake check` as the gate, all gone); companion Plan B (2026-07-05-machines-fleet-layout-B-backup.md) is another worker's row and was an independent sibling, not an alternative. | nothing found |
| `docs/superpowers/plans/2026-07-07-fleet-mesh-vpn-ssh.md` | rewrite | IMPLEMENTED then RETIRED — 1ce09bf (2026-07-17) removed the AWG mesh and the roadmap's Done section records it; the doc is already labelled superseded at docs/superpowers/specs/2026-07-08-unified-fleet-provisioner-design.md:5 and cited as a history source at docs/fleet-mesh-history.md:161, and no plan or spec has ever been retired by deletion in this repo, so a status header is the fix, not `delete`. | docs/fleet-mesh-history.md:161; docs/superpowers/specs/2026-07-08-unified-fleet-provisioner-design.md:5 |
| `docs/superpowers/plans/2026-07-07-fleet-onboarding.md` | rewrite | IMPLEMENTED — `provision/` exists as the top-level tier with `README.md`, `repos.sh`, `linux.sh`, `windows.ps1`, and the `~/my`·`~/pure`·`~/cyphy671` layout is the live one; stale only in naming `hosts/g16/windows/*` (renamed to `hosts/desktop` by the 2026-07-20 normalization) and `provision/repos.txt`, which was never created. | nothing found |
| `docs/superpowers/plans/2026-07-08-composable-personality-memory.md` | rewrite | IMPLEMENTED (f146356, 2026-07-08, created agents/memory/personality/tone.md and the other three facets) then the entire store left this repo — 87bf673 (2026-07-28) 'delete agents/memory/ — the store now lives in dotfiles'; the facets are live at ~/.claude/memory/personality/, so the header should point at 2026-07-28-agent-config-to-dotfiles.md. | nothing found |
| `docs/superpowers/plans/2026-07-08-fleet-provisioner-phase1-manifest-dispatcher.md` | rewrite | IMPLEMENTED and still load-bearing — `fleet.json` plus `provision/{provision.sh,provision.ps1,lib/fleet.sh,lib/Fleet.psm1}` are all live; but the plan's own "prints the plan, applies nothing" framing survives verbatim as a stale comment at `provision/provision.sh:3` even though `--apply` shipped in phase 2, which is P5's drift cost showing up in code rather than docs. | docs/fleet-mesh-history.md:180; docs/superpowers/specs/2026-07-08-fleet-provisioner-phase2-agents-executor-design.md:6 |
| `docs/superpowers/plans/2026-07-08-fleet-provisioner-phase2-agents-executor.md` | rewrite | IMPLEMENTED and still load-bearing — provision/roles/agents.sh (8087182) and agents.ps1 exist, agents/bootstrap.sh carries 15 DRY_RUN references, and the per-role confirm dispatcher this phase established is what roles/dotfiles.* and roles/repos.* still reuse; stale only in its nixos-no-op branch and its `homeserver` runbook. | nothing found |
| `docs/superpowers/plans/2026-07-08-fleet-provisioner-phase3-dotfiles-executor.md` | rewrite | IMPLEMENTED then SUPERSEDED — the chezmoi seed `dotfiles/dot_gitconfig.tmpl` was really added (b9fa0e6) and later removed; `provision/roles/dotfiles.sh:1-7` now explicitly repudiates the model ("No symlinks, no render step, no chezmoi") and cites the bare-repo spec instead. Superseded by docs/superpowers/plans/2026-07-28-dotfiles-private-bare-repo.md; a reader who trusts this file will look for chezmoi that is gone on purpose. | nothing found |
| `docs/superpowers/plans/2026-07-08-fleet-provisioner-phase4-repos-executor.md` | rewrite | IMPLEMENTED as a pure wrap exactly as specified — provision/roles/repos.sh (201e6d3) and repos.ps1 exist and provision/repos.sh is unedited; only its `homeserver`/`g614jv` runbook names and Git-Bash-on-Windows framing have aged. | nothing found |
| `docs/superpowers/plans/2026-07-08-fleet-provisioner-phase5a-nix-single-source.md` | rewrite | IMPLEMENTED then subject DELETED — the g16 removal half landed as 73d8276 ("phase5a: remove retired NixOS g16") and the single-source half as 7a729a0 plus generated matchBlocks (`nixos-final:modules/home/ssh.nix:8` reads "GENERATED from fleet.json"), after which f3d63b2 deleted the whole Nix tree; every non-doc path it names is now MISSING. Header should point at docs/2026-08-01-nixos-harvest.md. | nothing found |
| `docs/superpowers/plans/2026-07-09-fleet-provisioner-phase5b-mesh-executors.md` | rewrite | IMPLEMENTED then every artefact deleted in one commit — provision/roles/mesh-member.sh, roles/mesh-hub.sh, lib/mesh.sh and lib/Mesh.psm1 were all removed by 1ce09bf (2026-07-17); a status header matters more here than elsewhere because its sole citation, docs/fleet-mesh-history.md:353, still lists its Task 7 Step 3 as 'Real-box pending' for code that no longer exists. | docs/fleet-mesh-history.md:353 |
| `docs/superpowers/plans/2026-07-09-gortex-align-type-agnostic.md` | rewrite | IMPLEMENTED, and the artefact has since left this repo — 25bad2d (2026-07-09, the plan's own date) did exactly its Architecture: dropped `pyright-reference.md` + `pyrightconfig.json`, added `type-governance-reference.md`; the skill then moved out wholesale in 1b0414c "move the three untested skills to dotfiles" and is now the unprefixed `gortex-align` in the live skill list, i.e. no longer `machines` content at all. | nothing found |
| `docs/superpowers/plans/2026-07-13-headscale-fleet-mesh-probe.md` | rewrite | EXECUTED AND PASSED — its own Task-8 deliverable docs/superpowers/plans/2026-07-13-headscale-probe-results.md exists and opens with 'Verdict: PASS', and docs/fleet-roadmap.md's Done section records the 2026-07-13 rollout that followed; needs a one-line header pointing at the results file. | docs/superpowers/plans/2026-07-13-headscale-probe-results.md:7 |
| `docs/superpowers/plans/2026-07-13-headscale-fleet-rollout.md` | rewrite | IMPLEMENTED — the Headscale tailnet (`cc.cyphy.kz`, MagicDNS `gg.ez`) is the fleet's live transport per AGENTS.md; but its NixOS half (`modules/home/ssh.nix`) is gone with f3d63b2 and it still speaks of the "homeserver" and the AWG cutover, both retired names/mechanisms. | docs/fleet-mesh-history.md:13 |
| `docs/superpowers/plans/2026-07-14-fleet-ssh-over-tailnet-and-hosts.md` | rewrite | IMPLEMENTED then half deliberately RETIRED — the `hosts` role executors did land (9a1c062 posix, 2bba524 Windows, 88ad2f2 NixOS `networking.hosts`) and were then removed in 52b66d8 "retire the hosts-file machinery — MagicDNS is the resolver"; `provision/roles/` today holds only agents/dotfiles/repos, so this plan describes a role that was built and then decided against. | nothing found |
| `docs/superpowers/plans/2026-07-15-fleet-rename-layer1-repo-labels.md` | rewrite | IMPLEMENTED — fleet.json now keys on hub/latitude/desktop exactly as specified (and `server` left the manifest 2026-08-01), and docs/fleet-mesh-history.md:88 cites this plan as the branch/PR record; its Nix acceptance gate and `mesh.*`-preservation constraints describe machinery that no longer exists. Sibling layer2-3 plan is another worker's row. | docs/fleet-mesh-history.md:88 |
| `docs/superpowers/plans/2026-07-15-fleet-rename-layer2-3-headscale-and-magicdns-cleanup.md` | rewrite | IMPLEMENTED — `hosts/latitude` exists and `hosts/latitude5520` does not, and MagicDNS is the fleet-wide resolver; its remaining targets (`provision/roles/hosts.*`, `modules/system/fleet-hosts.nix`, `modules/home/ssh.nix`) were retired in 52b66d8 and f3d63b2, so nothing in it is actionable. | nothing found |
| `docs/superpowers/plans/2026-07-15-orca-serve-wsl.md` | rewrite | HALF-LIVE, HALF-REVERTED: Task 1's artefact provision/tailscale-wsl.sh (1a0ef4a, 2026-07-16) survives and is step 1 of the `just provision-wsl` chain, but Task 2's provision/orca-serve.sh (475cbbe) was deleted 2026-07-21 by f95cb3f 'Orca runs on Windows now' — the header must say which half stands or a reader will re-derive the dead orca-serve/6768/pairing-URL design. | nothing found |
| `docs/superpowers/plans/2026-07-17-fleet-ssh-tailnet-retire-awg.md` | rewrite | PARTIAL, and the unfinished half is invisible — the AWG-retirement half is provably done (`provision/lib/mesh.sh`, `Mesh.psm1`, `roles/mesh-{hub,member}.*`, `provision/mesh-authorized-keys` and `modules/system/mesh-vpn*.nix` are all gone; `provision/fleet-authorized-keys` replaced them), but the sshd re-homing half is NOT: there is no `provision/roles/ssh-server` executor at all and the role is still a stub (roadmap P3). Its residual steps 3 and 6 are `nix flake check` against the flake deleted in f3d63b2, so it cannot be resumed from this file — the live successor is the handoff below. | nothing found (its successor work is docs/superpowers/handoffs/2026-07-17-fleet-ssh-over-tailnet-handoff.md) |
| `docs/superpowers/plans/2026-07-17-research-orchestrator.md` | rewrite | IMPLEMENTED, contrary to first appearances — both agents/subagents/research-orchestrator.md and agents/subagents/web-research.md exist and agents/bootstrap.sh deploys subagents entry-by-entry; only its packaging half (modules/home/claude.nix wiring) is dead, and it names the wrong branch (`Launching-subagents-call-stack`). | nothing found |
| `docs/superpowers/plans/2026-07-17-ssh-wsl-fleet.md` | rewrite | IMPLEMENTED, and the repo already says so in prose — `provision/ssh-wsl.sh` and `provision/ssh-wsl.test.sh` both exist, and the handoff states at L19-22 "That feature is **DONE and merged** — do not redo it." Only the plan file itself does not know; all 12 boxes are unchecked. | docs/superpowers/handoffs/2026-07-17-fleet-ssh-over-tailnet-handoff.md:21 |
| `docs/superpowers/plans/2026-07-17-tailscale-wsl-self-enroll.md` | rewrite | IMPLEMENTED to the letter — provision/tailscale-wsl.sh defines ts_extract_key_json at line 97 and ts_mint_key at line 107, carries four `--enroll` references, and provision/tailscale-wsl.test.sh is one of the 28 green suites; stale only in its 'no shellcheck on this NixOS host' verification note. | nothing found |
| `docs/superpowers/plans/2026-07-18-personal-git-workflow.md` | rewrite | IMPLEMENTED — `agents/docs/git-workflow.md` added 76f7b0e (2026-07-18), plus `agents/plugin/hooks/worktree-workflow.sh` and three suites under `agents/plugin/hooks/tests/` that `just test` runs today; the only MISS is `agents/memory/global.md`, which moved to dotfiles by design. | nothing found |
| `docs/superpowers/plans/2026-07-18-profile-provisioning-unification.md` | rewrite | PARTIALLY implemented and partly REVERSED — bootstrap.sh is indeed the single deployer today (AGENTS.md says so), but the nix half it depends on (claude.nix/codex.nix activation, MACHINES_HOST_ID from networking.hostName) died with the flake, and its headline goal 'retire the dead ~/.dotfiles husk' was undone ten days later by 2026-07-28-dotfiles-private-bare-repo.md, which recreated a bare repo at that exact path. | nothing found |
| `docs/superpowers/plans/2026-07-19-fleet-gather-windows.md` | rewrite | IMPLEMENTED and fully accurate — every path it names resolves (`agents/plugin/skills/kb-refresh/{SKILL.md,fleet-gather.sh,tests/test_fleet_gather.sh}`, `provision/ssh-wsl.sh`, its design spec); zero dead references, so a status header is literally all it needs. | nothing found |
| `docs/superpowers/plans/2026-07-19-fleet-kb-harvester.md` | rewrite | IMPLEMENTED — agents/plugin/skills/kb-refresh/ holds distill.py, fleet-gather.sh, SKILL.md and tests/ exactly as the File Structure section specifies, and `cyphy:kb-refresh` is a registered live skill; needs only a done header. | nothing found |
| `docs/superpowers/plans/2026-07-20-fleet-hostname-normalization.md` | rewrite | IMPLEMENTED — `hosts/desktop/windows/install.ps1` exists while `hosts/g16` and `hosts/homeserver` are gone, and `fleet.json` carries the model-derived `detect.hostname` values. Note it is also the source of the repo convention at L17 ("NEVER touch docs/superpowers/plans/**") that any status-header sweep must be reconciled with — see group notes. | nothing found |
| `docs/superpowers/plans/2026-07-20-orca-setup-skill.md` | rewrite | IMPLEMENTED — agents/plugin/skills/orca-setup/ exists and `cyphy:orca-setup` is a registered live skill whose description matches the plan's goal verbatim (scaffold .orca/worktree-setup.sh, print the one-liner, never write orca-data.json); needs only a done header. | nothing found |
| `docs/superpowers/plans/2026-07-20-ship-fleet-sync-skill.md` | rewrite | IMPLEMENTED — `agents/plugin/skills/ship/{SKILL.md,fleet-pull.sh,tests/fleet-pull.test.sh}` added 268a992 (2026-07-20) and `/ship` is a registered live skill; every path it names exists. | nothing found |
| `docs/superpowers/plans/2026-07-21-fleet-converge-self-healing-sync.md` | rewrite | IMPLEMENTED — scripts/converge.sh plus converge.test.sh, agents/git-hooks/post-merge and provision/fleet-selfpull.{sh,ps1} are all present; its NixOS tier (machines-converge.path unit, modules/system/*.nix, rebuild against flake.lock) is dead, though converge.sh's `nixos` class detection is the deliberate fail-safe leftover. This is the origin doc for the fourth git-sync mechanism the review flagged. | nothing found |
| `docs/superpowers/plans/2026-07-22-fleet-reach-every-clone.md` | rewrite | IMPLEMENTED — `agents/plugin/skills/lib/fleet-dispatch.sh` plus its test, `provision/fleet-local.sh` (45e456f, 2026-07-22) plus `provision/tests/fleet-local.test.sh`, and the WSL trio all exist; AGENTS.md documents `fd_probe`/`fd_run`/`fd_wsl_hosts` as the shared primitive. Carries the group's worst signal-to-truth ratio: 66 checkboxes, 0 checked, on finished work. | nothing found |
| `docs/superpowers/plans/2026-07-25-hub-fleet-enrollment-tiers.md` | rewrite | IMPLEMENTED — provision/lib/tiers.sh exists (the 1435-line, 21-function file the review flagged as holding essentially all behaviour) and fleet.json carries `"profile": "hub"` at line 27 and `"profile": "server"` at line 5; this plan is the sole written rationale for the profile→tier-list design, so its header should mark it done rather than let it read as pending. | nothing found |
| `docs/superpowers/plans/2026-07-27-fleet-migration-mac-primary-latitude-server.md` | rewrite | IMPLEMENTED (12/120 boxes checked) — air is the primary dev box and latitude the services host, and `provision/macos.sh` + `just provision-mac` exist; roadmap P5 line 369 explicitly names this as one of "the two migration plans that are effectively complete" and asks for exactly this status line. | .claude/memory/project.md:1392; docs/fleet-roadmap.md:257; docs/superpowers/specs/2026-07-27-macos-one-command-provisioning-design.md:9 |
| `docs/superpowers/plans/2026-07-28-agent-config-to-dotfiles.md` | rewrite | IMPLEMENTED — 87bf673 (2026-07-28) deleted agents/memory/ and the stores now live at ~/.claude/memory/* under dotfiles, exactly as planned; but its 'Follow-ups deliberately not in this plan' block still holds two live items (`hermes/` and the agents/settings*.json ownership question) that grep finds nowhere in docs/fleet-roadmap.md, so the rewrite must promote them before marking the plan done. | nothing found |
| `docs/superpowers/plans/2026-07-28-dotfiles-private-bare-repo.md` | rewrite | IMPLEMENTED (8/79 boxes checked) — the bare repo is the live operating model documented in $HOME/CLAUDE.md, and `provision/dotfiles-sync.{sh,ps1}` + test, `agents/plugin/hooks/dotfiles-offer.sh` + test and `provision/roles/dotfiles.sh` all exist. The other of P5:369's "two migration plans effectively complete", and at 2653 lines the largest plan in the repo. | nothing found |
| `docs/superpowers/plans/2026-07-28-dotfiles-sync-commit-debounce.md` | rewrite | IMPLEMENTED — provision/dotfiles-sync.sh has 5 sync_should_commit/DOTFILES_SYNC_FORCE references and the .ps1 mirror has 2, provision/dotfiles-sync.test.sh is one of the 28 green suites, and `/dotfiles-sync` is a registered live skill; needs only a done header. | nothing found |
| `docs/superpowers/plans/2026-08-01-wsl-two-distro-machinery.md` | rewrite | IMPLEMENTED despite being the newest and 0/38 checked — `provision/wsl-fixes.sh` (67fd89e), `provision/assets/wslopen` (eee412b), `fleet-local.sh --dispatch` and `provision-wsl.sh --no-tailscale` all exist, with four suites in `just test`, and AGENTS.md documents the live chain; the still-open half is its companion rollout plan, which declares this one a merged prerequisite. | docs/superpowers/plans/2026-08-01-wsl-two-distro-rollout.md:40 |
| `docs/superpowers/specs/2026-06-30-codex-support-design.md` | rewrite | Header "Status: Implemented" is true of the capability (agents/bootstrap.sh still links ~/.codex) but every mechanism it names is dead vocabulary — the `claude/` source dir (now `agents/`), a `codex/` dir that does not exist, `codex.nix`, and the Verification section's `just quick` / `just check`, none of which are in the 16-recipe justfile; needs a supersede banner like the phase3 spec has. | docs/superpowers/plans/2026-06-30-codex-support.md:570 only |
| `docs/superpowers/specs/2026-07-02-agents-rename-per-profile-bootstrap-design.md` | rewrite | Shipped (agents/, agents/bootstrap.sh, `just agent-bootstrap-profile`) yet line 4 still reads "approved (pending spec review)", and its §112 "Nix + reference updates" half (modules/home/claude.nix, codex.nix) died with f3d63b2 while the ~/.claude-work profile it centres on was folded back into settings.json in d48c09a. | docs/superpowers/specs/2026-07-04-cyphy-plugin-design.md:8 |
| `docs/superpowers/specs/2026-07-02-unified-profile-settings-sharing-design.md` | rewrite | Status line still promises `settings.personal.json` + `settings.work.json` with "Nix owns both profiles"; the tree has only agents/settings.json (AGENTS.md records the pure profile folded back in d48c09a) and no Nix — but its "Correction (2026-07-02)" table is the empirical record that a config-dir-root settings.local.json is never read, a finding since absorbed into ~/.claude/CLAUDE.md, so this is a banner job not a content rescue. | docs/superpowers/plans/2026-07-02-unified-profile-settings-sharing.md:11 only |
| `docs/superpowers/specs/2026-07-04-cyphy-plugin-design.md` | rewrite | The plugin shipped (agents/plugin/.claude-plugin/plugin.json, hooks/hooks.json, five skills) but line 4 says "approved (pending spec review)" and its step-8 validation is `just quick` / `nix flake check`, neither of which exists since the flake was deleted in f3d63b2. | nothing found |
| `docs/superpowers/specs/2026-07-05-machines-fleet-layout-design.md` | rewrite | Its "fleet (ground truth)" table is g16 / homeserver / latitude5520 and its target tree is flake.nix + modules/ + pkgs/ + a top-level backup/ — the flake is gone (f3d63b2) and there is no backup/ dir in the repo (the restic clients went back to ~/my/vps); only §2b (why install-media/ is shared and flat, why autounattend.xml was git-mv'd not copied) still describes the live tree. | plans/2026-07-05-machines-fleet-layout-{A-reorg,B-backup}.md; decision #9 superseded in-prose by 2026-07-07-fleet-onboarding-design.md:9 |
| `docs/superpowers/specs/2026-07-07-composable-personality-memory-design.md` | rewrite | The four-facet personality/ split shipped but RELOCATED — the facets now live in the dotfiles bare repo at ~/.claude/memory/personality/ per the global CLAUDE.md, and `ls agents/` shows no memory/ dir at all; its whole Wiring section (modules/home/claude.nix, codex.nix, `just switch`) targets the deleted NixOS tree. Superseded in effect by 2026-07-28-dotfiles-private-bare-repo-design.md with no banner saying so. | docs/superpowers/plans/2026-07-08-composable-personality-memory.md:11 only |
| `docs/superpowers/specs/2026-07-07-fleet-onboarding-design.md` | rewrite | The provision/ tier it created is the live architecture (provision/linux.sh, windows.ps1, repos.sh all present) and this is the only record of WHY it is called `provision/` (the triple-"bootstrap" name collision), but decision #10's committed `provision/repos.txt` was silently dropped — the file does not exist and neither provision/repos.sh nor provision/roles/repos.sh greps for it; the box table is g16 / homeserver / methe-server / ~/gh throughout. | docs/superpowers/plans/2026-07-07-fleet-onboarding.md (same-dated sibling) |
| `docs/superpowers/specs/2026-07-08-fleet-provisioner-phase2-agents-executor-design.md` | rewrite | Fully shipped — provision/roles/agents.{sh,ps1} exist, bootstrap.sh has DRY_RUN, provision/tests/roles.test.sh covers it — yet the header block still says "pending user review of this written spec, then → writing-plans", and its runbook targets `homeserver` and the nixos no-op, a fleet that no longer exists. | docs/superpowers/plans/2026-07-08-fleet-provisioner-phase2-agents-executor.md:11 |
| `docs/superpowers/specs/2026-07-08-fleet-provisioner-phase4-repos-executor-design.md` | rewrite | Shipped as provision/roles/repos.{sh,ps1} wrapping provision/repos.sh unchanged, but §2 still asserts "`repos` is on `latitude5520` (nixos)" and §8 excludes `methe-server` — latitude is Debian since the 2026-08 reinstall and `server` left fleet.json 2026-08-01. | docs/superpowers/plans/2026-07-08-fleet-provisioner-phase4-repos-executor.md:11 |
| `docs/superpowers/specs/2026-07-14-fleet-ssh-over-tailnet-and-hosts-design.md` | rewrite | Half-shipped, half-retired and marked as neither: the SSH-alias half is live but relocated (provision/lib/tiers.sh:1041 `tier_fleet_ssh` replaced modules/home/ssh.nix, deleted in f3d63b2), while the `hosts`-role half it stages as Stage 2 was explicitly retired by 52b66d8 "retire the hosts-file machinery — MagicDNS is the resolver". | docs/superpowers/plans/2026-07-14-fleet-ssh-over-tailnet-and-hosts.md:11; docs/fleet-mesh-history.md:39 |
| `docs/superpowers/specs/2026-07-17-orca-worktree-setup-design.md` | rewrite &rarr; `docs/superpowers/specs/2026-07-20-orca-setup-skill-design.md` | Both components it specifies are absent at the stated paths — no scripts/orca-worktree-setup.sh (the live dispatcher is agents/worktree-setup.sh) and no scripts/orca-worktree.d/backend-api.sh, because the central-delegate model was replaced by the committed per-repo .orca/worktree-setup.sh of 2026-07-20-orca-setup-skill-design.md; needs the same SUPERSEDED banner the repo already gives phase3-dotfiles-chezmoi. | nothing found |
| `docs/superpowers/specs/2026-07-18-personal-git-workflow-design.md` | rewrite | Its Problem section calls the ~/.dotfiles bare repo "a *different, now-dead* model" and its out-of-scope list proposes "physically retiring the ~/.dotfiles husk" — inverted since 2026-07-28 made that bare repo the fleet's live $HOME engine, so an agent reading this spec is actively misled about a running system. | agents/docs/git-workflow.md (guarded by agents/plugin/hooks/tests/git-workflow-doc.test.sh) |
| `docs/superpowers/specs/2026-07-18-profile-provisioning-unification-design.md` | rewrite | HIGHEST-RISK ROW. Half A shipped (bootstrap.sh is the sole deployer; MACHINES_HOST_ID is live at agents/bootstrap.sh:349), but Half B is a per-machine checklist instructing `rm -rf ~/.dotfiles` and "delete the stale ~/CLAUDE.md" — both paths exist right now on this box and are load-bearing, because the private bare repo was deliberately REVIVED by 2026-07-28-dotfiles-private-bare-repo-design.md. Roadmap P5 measures that agents do act on these docs (four re-derivations in one session, twice wrong first). Its plan:291 also says the spec holds a per-box completion checklist; the spec contains no tick marks, so nothing was ever tracked. | docs/superpowers/plans/2026-07-18-profile-provisioning-unification.md:291 |
| `docs/superpowers/specs/2026-07-19-fleet-kb-harvester-design.md` | rewrite | Shipped as agents/plugin/skills/kb-refresh/ (SKILL.md, distill.py, two test files) but line 5 still says "design approved, pending spec review → implementation plan", its fleet roster is "g16 / desktop, latitude, server" (g16 retired 2026-07-08, server left fleet.json 2026-08-01), and its "catch-up run (now)" section describes a one-off that already happened. | nothing found |
| `docs/superpowers/specs/2026-07-20-orca-setup-skill-design.md` | rewrite | Shipped as agents/plugin/skills/orca-setup/ (SKILL.md, orca-status.sh, worktree-setup.template.sh, two test suites) yet line 3 says "approved design, pending implementation plan", and its Problem section still names the dispatcher `scripts/orca-worktree-setup.sh`, a path that does not exist (it is agents/worktree-setup.sh). | nothing found |
| `docs/superpowers/specs/2026-07-21-fleet-converge-self-healing-sync-design.md` | rewrite | The design shipped (scripts/converge.sh + converge.test.sh, agents/git-hooks/post-merge, provision/fleet-selfpull.{sh,ps1}) but its "Reused precedent (all in-repo)" list and its NixOS converge-unit prescription name modules/system/self-update.nix and modules/system/git-autofetch/, both deleted in f3d63b2 — the section reads as a live inventory and is not one. | docs/superpowers/plans/2026-07-21-fleet-converge-self-healing-sync.md:71 |
| `docs/superpowers/specs/2026-07-21-fleet-split-desktop-wsl-design.md` | rewrite | Shipped (agents/plugin/skills/lib/fleet-dispatch.sh, provision/provision-wsl.sh, provision/fleet-local.sh, `just provision-wsl` at justfile:97, tests/fleet-local.test.sh) but it is the design of record for fleet.local.json and its schema is now WRONG: the spec writes `{self:{nickname,fleet,platform}}` while provision/fleet-local.sh:35 writes `{nickname,fleet,platform,dispatch}`, and the whole `dispatch: direct\|parent` invariant (at most one direct distro per Windows host, since WSL2 distros share a network namespace) postdates it and appears only in AGENTS.md. | nothing found — no plan, code or doc cites this path |
| `install-media/README.md` | rewrite | Describes a fleet that no longer exists: lines 4-6 scope the media to "the `g16` daily driver and the `homeserver` (`g513ie`, formerly `methe-server`)" — g16 was renamed `desktop` 2026-07-20, `homeserver` became `server`, and the `server` MACHINE left fleet.json 2026-08-01 with hosts/server/ deleted, so exactly one Windows box (desktop) remains; line 33 repeats "The g16 reinstall runbook". The deploy table and the not-tracked list are still accurate — only the audience paragraph is wrong. (No collision with the surviving `server` PROFILE: the stale reference is to the machine.) | README.md:35, AGENTS.md:48 and :154 describe install-media/; no code reads it |
| `justfile` | rewrite | The `test` recipe under-runs the gate and its own comment says so unknowingly: lines 55-56 glob four hand-listed directories, matching 28 of the repo's 38 *.test.sh files, while lines 45-47 claim "Runs every suite in the repo" and warn that a hand-listed set makes "coverage silently stop growing" — which is exactly what happened. The 10 unreachable suites are all 4 in agents/plugin/hooks/tests/, all 5 under agents/plugin/skills/*/tests/, and agents/git-hooks/post-merge.test.sh (the converge trigger's own test). | provision/tests/justfile.test.sh (asserts every recipe carries [group]+[doc]); README.md:59-72 and AGENTS.md:78-130 document the recipe set |
| `provision/README.md` | rewrite | 454 lines titled 'Non-Nix provisioning', a framing that is now vacuous: lines 17-19 sell parity with 'the NixOS laptops', line 388 instructs the operator to `nixos-rebuild switch` the other boxes, line 407 cites NixOS `authorizedKeys.keyFiles`, line 363 documents `ssh server` (left fleet.json 2026-08-01), and there is no Windows section at all although root README:44 sends Windows users to windows.ps1. | AGENTS.md:149; linux.sh:38; macos.sh:43; root README.md |
| `provision/fleet-selfpull.ps1` | rewrite | Line 47 is a bare `git pull --ff-only` — exactly the shape its POSIX twin removed (fleet-selfpull.sh:42-61) because it filed auth failures as 'SKIP diverged' and always exited 0; it also lacks the fetch-race retry (sh:51-61) and scans $USERPROFILE\GitHub while the .sh scans $HOME/my $HOME/pure $HOME/cyphy671 $HOME/exactly (sh:26). | provision/windows.ps1:395 |
| `provision/fleet-selfpull.test.sh` | rewrite | Lines 123-127 assert 'every scheduler must invoke the script THROUGH bash' against $HERE/../modules/system/fleet-selfpull.nix behind an `if [ -f ]` — modules/ was deleted 2026-08-01, so the block silently never fires while three LIVE schedulers (tiers.sh:1186/1189 launchd, :1207 systemd ExecStart, :1234 cron, all `/usr/bin/env bash`) depend on the mode-644 invariant unasserted. | just test — justfile glob `provision/*.test.sh`; the invariant's live consumers are provision/lib/tiers.sh tier_selfpull and provision/fleet-selfpull.ps1 (a native PS mirror, no bash involved) |
| `provision/lib/Fleet.psm1` | rewrite | Missing the unknown-machine guard its bash twin has: fleet.sh:43-55 documents that an unknown name makes roles empty and the run exit 0, and provision.ps1:40-44 walks exactly that path with no Get-FleetHasMachine — plus zero test coverage (grep for Fleet.psm1 across provision/tests/, agents/tests/, scripts/ returns nothing found). | provision/provision.ps1:10 (Import-Module), :27 Get-FleetDetected, :40 Get-FleetPlatform, :44 Get-FleetRoles; provision/roles/dotfiles.ps1:26-28 (comment only) |
| `provision/lib/tiers.sh` | rewrite | Live and load-bearing but carries three concrete defects: _launchd_service (:79-86, 14 lines) is dead — zero references anywhere in the repo, not even a test; the `dotfiles sync timer — spec 2026-07-28 §5.4` docblock at :1262-1274 is orphaned from tier_dotfiles_sync() at :1334 by the whole tier_sudo_nopasswd section (:1275-1333), so one tier has no doc and the other appears to carry two headers; and the systemd-user + cron install branches are triplicated near-verbatim across tier_autofetch (:862-902), tier_selfpull (:1193-1240) and tier_dotfiles_sync (:1343-1380) while only the launchd branch was factored into _launchd_periodic — that asymmetry, not the duplication alone, is the finding. | provision/linux.sh:171 and provision/macos.sh:134 (dynamic "tier_${_call[0]}" dispatch off the profile TIERS array); provision/roles/dotfiles.sh:252 calls tier_dotfiles_sync directly; provision/tests/tiers.test.sh and fleet-ssh-tier.test.sh source it |
| `provision/linux.sh` | rewrite | The profile→tier tables (67-118) are current and correct, but the header sells the file as 'the imperative, apt-based counterpart to the NixOS hosts (hosts/*/nixos/)' (line 20) — a directory that no longer exists — line 124 says the dry run is inspectable 'including a NixOS one', and the closing banner at 185-186 tells every operator 'only a NixOS host gets these'. | provision/provision-wsl.sh:32; provision/README.md:7; root README.md:43 |
| `provision/macos.sh` | rewrite | Live driver (provision-mac.sh:81) whose tier list is correct, but lines 31, 89 and 154-155 still frame it against 'the NixOS hosts' / 'the NixOS one this repo is usually edited on' / 'only a NixOS host gets these' — this repo is edited on air, and there is no Nix box left to be the counterpart of. | provision/provision-mac.sh:57,81; provision/README.md:205 |
| `provision/roles/agents.ps1` | rewrite | Lines 13-16 branch on $Platform -eq 'nixos' and tell the operator the config is 'owned by home-manager — applied by just switch'; fleet.json has no platform:nixos machine so the arm is unreachable, and both claude.nix and the `just switch` recipe were deleted 2026-08-01. Lines 26-31 also duplicate repos.ps1:23-28 verbatim. | dot-sourced by provision/provision.ps1:13-14,19 — itself uninvoked, see that row |
| `provision/roles/agents.sh` | rewrite | Same dead branch, with behavioural consequence: lines 20-23 return 0 without running bootstrap.sh for platform 'nixos', and 5-7 state that rule as fact. No fleet.json machine has that platform. Coupled edit required: provision/tests/roles.test.sh:40-42 pins this arm ('role_agents: nixos arm changed') and will go red unless changed in the same commit. | sourced by provision/provision.sh:11-15; provision/tests/roles.test.sh:27,29,40 |
| `provision/ssh-wsl.sh` | rewrite | Load-bearing twice over (provision-wsl.sh step 2, and tier_fleet_ssh at lib/tiers.sh:1041-1055 sources its pure helpers via SSH_WSL_LIB_ONLY), but line 328 prints an operator instruction to 'nixos-rebuild switch' the other boxes — actionably wrong, not merely stale prose — and lines 5 and 44 promise `ssh server`, a machine that left fleet.json 2026-08-01. | provision/provision-wsl.sh:31; provision/lib/tiers.sh:1041 (tier_fleet_ssh); provision/tests/fleet-ssh-tier.test.sh |
| `provision/tests/justfile.test.sh` | rewrite | It guards the justfile's HELP text but not the gate's own coverage, and the gate has silently lost 10 suites: `find . -name '*.test.sh'` returns 38, the `test` recipe's four globs match 28 — everything under agents/plugin/**/tests/ plus agents/git-hooks/post-merge.test.sh never runs, which is precisely the drift the recipe comment ('a new *.test.sh must be picked up without editing this recipe, or coverage silently stops growing') claims to prevent. | just test — justfile glob `provision/tests/*.test.sh`; subject is the repo-root justfile. SKIPs entirely (exit 0) when `just` is absent, e.g. on hub |
| `provision/tests/provision-mac.test.sh` | rewrite | Line 5 reads 'These run on the NixOS dev box, so the privileged and Darwin-only paths cannot execute' — there is no NixOS host (deleted 2026-08-01) and the dev box is now air, a Mac, so the stated reason the Darwin paths are unrunnable is false; the assertions themselves are correct and green, only the premise needs restating. | just test — justfile glob `provision/tests/*.test.sh`; subjects are provision/provision-mac.sh, macos-prep.sh and tailscale-mac.sh |
| `provision/tests/tiers.test.sh` | rewrite | The central suite (294 lines pinning both drivers' full tier lists) but three comments describe the deleted world: line 215 'the tier list is inspectable from this NixOS box', and lines 222-223 'NixOS gets its fleet client config from modules/home/ssh.nix' — modules/ was removed 2026-08-01, so the stated justification for tier_fleet_ssh being darwin-only now cites a file that does not exist; assertions are correct and green. | just test — justfile glob `provision/tests/*.test.sh`; subjects are provision/linux.sh, provision/macos.sh and provision/lib/tiers.sh |
| `.claude/kb-harvest-state.json` | keep | Live state file for the kb-refresh skill: SKILL.md:31 declares it git-tracked on purpose, :155 is the writer, and kb-cron-prompt.md:60/198/208 read and commit it; its `tiers_touched` naming now-deleted agents/memory/global.md and agents/hosts/g513ie.md is a historical log of the 2026-07-24 refresh, not a wrong claim about today. | agents/plugin/skills/kb-refresh/SKILL.md:31,41,155; agents/docs/kb-cron-prompt.md:60,198,208,240 |
| `.claude/settings.json` | keep | 7 lines, one permission entry allowing `mcp__gortex__*`, matching the gortex MCP server that .mcp.json actually registers for this repo; nothing stale and nothing to trim. | Claude Code harness (project-scope settings); pairs with .mcp.json's gortex server |
| `.mcp.json` | keep | 13 lines and genuinely live: agents/plugin/hooks/gortex-onboard-check.sh:44 greps it (`grep -qs '"gortex"' "$root/.mcp.json"`) to decide whether to nudge, and agents/README.md:414 / bootstrap.sh:543 document it as what brings the gortex daemon up per session with no explicit start step. | agents/plugin/hooks/gortex-onboard-check.sh:44; agents/bootstrap.sh:543; agents/README.md:414 |
| `CLAUDE.md` | keep | Verified a true git symlink, not a duplicated file — `git ls-files -s CLAUDE.md` returns mode `120000` pointing at AGENTS.md, exactly the contract AGENTS.md:4-5 states ("edit AGENTS.md, never create a second real file at the CLAUDE.md path"). | Claude Code memory discovery at repo root; contract asserted in AGENTS.md:4-5 |
| `agents/.gitattributes` | keep | 5 lines, `* text eol=lf`; load-bearing for the Git Bash members (desktop / desktop-wsl) where a CRLF checkout breaks `#!/usr/bin/env bash` in the 82 shell scripts this repo ships. | git checkout itself (attribute file); no script reads it |
| `agents/bootstrap.sh` | keep | 686 lines of live, tested deployer — invoked by `just agent-bootstrap`/`agent-bootstrap-profile`, by git-hooks/_refresh-claude-config on every pull, and it self-installs core.hooksPath (verified live: `git config --local core.hooksPath` = /Users/me/machines/agents/git-hooks); bootstrap.test.sh green. Caveat, not a verdict change: comments at lines 5-6, 348, 359, 533-534 and 572-576 still name `modules/home/claude.nix` / `pkgs/gortex.nix` as a co-equal live mechanism, sending a reader after a deleted tree — the `[ -e /etc/NIXOS ]` guards themselves are the protected fail-safe leftover. | justfile (agent-bootstrap, agent-bootstrap-profile), agents/git-hooks/_refresh-claude-config:29, provision/windows.ps1 |
| `agents/docs/claude-code-subagents.md` | keep | Current, well-sourced 130-line reference (9 upstream doc URLs, verified 2026-07) and the only written spec for why `web-research` carries `disallowedTools: Agent`; single stale clause at line 98 says the subagents are linked "by `bootstrap.sh` / `claude.nix`" — claude.nix is gone, bootstrap.sh:522 is now the sole linker. | agents/plugin/skills/kb-refresh/SKILL.md |
| `agents/docs/git-workflow.md` | keep | Not a passive doc — agents/plugin/hooks/worktree-workflow.sh:83,93 `sed`s the `<!-- WORKTREE-MODE:START/END -->` span out of this exact file at SessionStart, and git-workflow-doc.test.sh:6-8 asserts the markers exist; deleting or reflowing it breaks a live hook. | agents/plugin/hooks/worktree-workflow.sh:83, agents/plugin/hooks/tests/git-workflow-doc.test.sh:6, plugin/skills/ship + kb-refresh SKILL.md |
| `agents/docs/kb-cron-prompt.md` | keep | Copy-paste prompt with no code caller by design, and actively maintained — last edited 2026-08-01 (50c80ff) specifically to record Hermes leaving the fleet; it and kb-reflection-prompt.md are explicit counterparts (append-only harvest vs. the only job allowed to delete). | .claude/memory/project.md and agents/docs/kb-reflection-prompt.md; no script invokes it (pasted into an Orca cron job) |
| `agents/docs/kb-reflection-prompt.md` | keep | Same shape as its counterpart and equally current (50c80ff, 2026-08-01); it is the only written record that the reflection job's corpus moved to the dotfiles bare repo on 2026-07-28 (87bf673, 6364b31) and that it must never `checkout main`. | .claude/memory/project.md and agents/docs/kb-cron-prompt.md; pasted into a cron job, no script caller |
| `agents/git-hooks/_refresh-claude-config` | keep | The shared body all three hook entrypoints exec; its `env -u CLAUDE_CONFIG_DIR` at line 29 fixes a real incident dated in-file 2026-08-01 (a pull typed in an Orca terminal bootstrapped Orca's `auth` dir as a secondary profile and clobbered the sync mirror) — that reasoning is not re-derivable. Has no test of its own. | agents/git-hooks/post-checkout, post-merge:14, post-rewrite; reached via core.hooksPath set by bootstrap.sh |
| `agents/git-hooks/post-checkout` | keep | 4-line entrypoint that execs _refresh-claude-config; live via core.hooksPath, and bootstrap.test.sh:16,175-180 explicitly passes `-c core.hooksPath=/dev/null` to suppress it during `worktree add`, which is direct evidence it fires. | git (core.hooksPath -> agents/git-hooks) |
| `agents/git-hooks/post-merge` | keep | The only hook that does two jobs (deliberately no `exec`, documented at line 3): config refresh plus the non-Nix convergence trigger, routing to `schtasks /run /tn machines-converge` on Git Bash and a detached setsid/nohup elsewhere — this is the 4th git-sync mechanism named in the seed findings. | git (core.hooksPath); scripts/converge.sh is its payload |
| `agents/git-hooks/post-merge.test.sh` | keep | Good test (stubs uname + the refresh script, asserts both jobs ran) and passes by hand — but it is NOT in the gate: the `just test` recipe globs only `provision/tests/`, `provision/`, `agents/tests/` and `scripts/`, so this file is one of 10 tracked `*.test.sh` the gate never reaches (28 run of 38 tracked). Either move it to agents/tests/ or widen the glob. | nothing found — not matched by any justfile glob |
| `agents/git-hooks/post-rewrite` | keep | 4-line entrypoint for rebase / `commit --amend`; completes the three-way coverage (merge, checkout, rewrite) so a pull done either way re-links config. Untested, like post-checkout and _refresh-claude-config. | git (core.hooksPath -> agents/git-hooks) |
| `agents/orca-profile-harvest.sh` | keep | Genuinely distinct from link.sh despite both answering profile-durability: this is a no-`--delete` one-way rsync OUT (archive semantics, deliberately keeps files the live profile deleted), and lines 196-198 explicitly detect an already-relocated profile and no-op rather than rsyncing a directory into itself — the two scripts are mutually aware, not accreted. Test green. | justfile `agent-harvest-orca`, agents/bootstrap.sh:629-631 (runs last, after sync) |
| `agents/orca-profile-link.sh` | keep | The stronger durability answer — relocates the profile to ~/.claude-profiles/<name> and symlinks Orca's dir back, pairing on the stable accountUuid rather than Orca's re-generated dir id; bootstrap.sh:619-621 runs `--relink` FIRST so a re-auth-broken link is healed before sync writes into it. Distinct mechanism, not a duplicate. Test green. | justfile `agent-link-orca`, agents/bootstrap.sh:619-621, cyphy orca-setup/orca-repair skills |
| `agents/orca-profile-sync.sh` | keep | The third leg and the only one that pushes config IN (curated PROFILE_FILES/PROFILE_DIRS symlinked from the live primary profile, settings.json merged rather than linked); bootstrap.sh:135-142 also execs it directly when the target dir is an Orca auth dir. Test green. | justfile `agent-sync-orca`, agents/bootstrap.sh:142 and :624, agents/orca-profile-link.sh |
| `agents/plugin/.claude-plugin/plugin.json` | keep | 7-line manifest that makes the dir load as `cyphy@skills-dir`; verified live — ~/.claude/skills/cyphy is a symlink to /Users/me/machines/agents/plugin, so this whole tree is deployed from the checkout. | Claude Code plugin loader; agents/README.md:51,55 |
| `agents/plugin/agents/quick-tasks.md` | keep | The plugin's only subagent, named in plugin.json:3 and documented at agents/docs/claude-code-subagents.md:107; minor drift risk only — it hardcodes a `Co-Authored-By: Claude Sonnet 4.6` trailer at lines 16 and 21 while its frontmatter just says `model: sonnet`. | agents/plugin/.claude-plugin/plugin.json:3; agents/docs/claude-code-subagents.md:107 |
| `agents/plugin/hooks/dotfiles-offer.sh` | keep | The only PostToolUse hook registered in hooks.json:26-33; the ban block (lines 52-58) and the gitignored-inside-a-repo branch (81-95) are both covered by its passing test. | agents/plugin/hooks/hooks.json:26-33 |
| `agents/plugin/hooks/global-memory-load.sh` | keep | SessionStart hook #2 (hooks.json:12-15); loads the dotfiles-tracked memory stores, and its take-config-dir-as-$1 design (lines 12-17) is load-bearing because the same file is symlinked at different depths for Codex and for this plugin. | agents/plugin/hooks/hooks.json:12-15 |
| `agents/plugin/hooks/gortex-onboard-check.sh` | keep | SessionStart hook #1 (hooks.json:7-10); ask-only, exits silently outside a git repo (line 20) and honours both opt-outs (~/.gortex-install-skip, .gortex-skip). | agents/plugin/hooks/hooks.json:7-10 |
| `agents/plugin/hooks/hooks.json` | keep | Bidirectional check passes: all 5 commands it registers (gortex-onboard-check, global-memory-load, project-memory-check, worktree-workflow, dotfiles-offer) exist in hooks/, and hooks/ contains no .sh that hooks.json fails to register — no orphan in either direction. | Claude Code plugin loader via ~/.claude/skills/cyphy |
| `agents/plugin/hooks/project-memory-check.sh` | keep | SessionStart hook #3 (hooks.json:16-19) and the actual loader for every repo's .claude/memory/project.md (line 33) — the mechanism notes-reconciled.test.sh:11-12 asserts against. | agents/plugin/hooks/hooks.json:16-19; agents/worktree-setup.sh:137 |
| `agents/plugin/hooks/tests/dotfiles-offer.test.sh` | keep | Passes when run by hand; builds a real bare dotfiles repo plus a sibling allowlist-gitignore repo so check-ignore behaves like production. Not collected by `just test` — see group_notes. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/hooks/tests/git-workflow-doc.test.sh` | keep | 16 lines guarding a real contract: the WORKTREE-MODE:START/END markers that worktree-workflow.sh:93 seds out, present at agents/docs/git-workflow.md:18 and :45. Passes. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/hooks/tests/notes-reconciled.test.sh` | keep | Passes; lines 14-22 record why the global-memory half was deliberately retired in 2026-07-29 (a machines test must not depend on a dotfiles-tracked $HOME path) — that rationale is worth keeping even though only two assertions remain. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/hooks/tests/worktree-workflow.test.sh` | keep | Passes; builds throwaway repos and real linked worktrees and asserts exit 0 plus empty stderr (assert_quiet_exit, lines 21-29) — the never-block-a-session contract the hook exists under. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/skills/kb-refresh/distill.py` | keep | Stdlib-only (no third-party imports, lines 7-11) precisely so it can be pushed to a remote box and run in place — fleet-gather.sh:118 cats it over ssh and fleet-gather.sh:69 runs it there. Read-once watermarking via resume_offset/identity_hash is the skill's core invariant. | agents/plugin/skills/kb-refresh/fleet-gather.sh:69,118; kb-refresh/SKILL.md:45,71 |
| `agents/plugin/skills/kb-refresh/fleet-gather.sh` | keep | Invoked verbatim at kb-refresh/SKILL.md:40; sources the shared dispatcher at line 11 and derives members from fleet.json at runtime (fleet_hosts, lines 16-28), which is why `server` leaving the manifest required no edit here. | agents/plugin/skills/kb-refresh/SKILL.md:40 |
| `agents/plugin/skills/kb-refresh/tests/test_fleet_gather.sh` | keep | Runs clean standalone (rc=0, "PASS: test_fleet_gather.sh") and covers fleet_hosts, detect_hosts and the WSL dispatch block; but its filename is `test_fleet_gather.sh`, not `*.test.sh`, so even a recursive-glob fix to Justfile:55-56 would still miss it. | nothing found — name does not match the repo's *.test.sh convention |
| `agents/plugin/skills/lib/fleet-dispatch.sh` | keep | Genuinely shared exactly as AGENTS.md:201 documents — sourced at ship/fleet-pull.sh:15 and kb-refresh/fleet-gather.sh:11, nowhere else; the Git-Bash-via-PowerShell workaround (lines 22-34) and the load-bearing `</dev/null` on fd_probe (line 38) are both non-obvious and both tested. | agents/plugin/skills/ship/fleet-pull.sh:15; agents/plugin/skills/kb-refresh/fleet-gather.sh:11 |
| `agents/plugin/skills/lib/tests/fleet-dispatch.test.sh` | keep | Passes; mocks ssh on $SSH and asserts the exact remote argv, and line 5's `exec </dev/null` deliberately turns a lost `</dev/null` in fd_probe into a visible hang. The `nixos` platform argument at lines 41/54/100 is an arbitrary token exercising the default POSIX-SSH arm, not a live claim about the fleet. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/skills/orca-repair/SKILL.md` | keep | Matches the shipped behaviour: its claim at lines 70-71 that --apply blocks only on the UI and that a lingering daemon is harmless is exactly what apply_should_block (orca-repair.py:200-212) implements; the per-platform path note at 77-82 records a real macOS bug that was fixed. | invoked as /cyphy:orca-repair |
| `agents/plugin/skills/orca-repair/tests/orca-repair.test.sh` | keep | Passes; loads orca-repair.py through importlib from a heredoc (lines 6-10) so it needs no pytest and no package layout — the exact pattern test_distill.py lacks, and the model for fixing it. | agents/plugin/skills/orca-repair/SKILL.md:87 |
| `agents/plugin/skills/orca-setup/orca-status.sh` | keep | Read-only status helper invoked at orca-setup/SKILL.md:89-101; the LEGACY-vs-CONFLICT distinction (lines 12-16, 76-86) exists so the wt-setup rename doesn't make every already-wired repo read as broken, and its read-only contract is asserted at tests/orca-status.test.sh:123. | agents/plugin/skills/orca-setup/SKILL.md:89,101 |
| `agents/plugin/skills/orca-setup/tests/orca-status.test.sh` | keep | Passes; covers every branch of the matrix (WIRED, LEGACY, UNWIRED, ABSENT, CONFLICT, repos[] fallback, missing file) and re-hashes the fixture at line 123 to prove the helper never wrote to it. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/skills/orca-setup/tests/template.test.sh` | keep | Passes; 33 lines guarding the never-block-Orca contract (no `set -e`, has `exit 0`, lines 18-19) and asserting the retired gortex-readiness block stays gone (lines 24-25) — a regression guard for a migration that already happened. | nothing found — outside the Justfile:55-56 glob |
| `agents/plugin/skills/orca-setup/worktree-setup.template.sh` | keep | Copied verbatim into a repo's .orca/ by orca-setup/SKILL.md:47-53; the managed marker block (lines 20-26) is the seam the skill is forbidden to regenerate (SKILL.md:38-39), and it is clean of the retired gortex handling. | agents/plugin/skills/orca-setup/SKILL.md:47,50,52 |
| `agents/plugin/skills/ship/SKILL.md` | keep | 52 lines, current: the thepureapp/ guard (line 18), the worktree-vs-main-checkout split, and delegation of the fleet half to fleet-pull.sh at line 41; no reference to server, NixOS or any retired mechanism. | invoked as /cyphy:ship |
| `agents/plugin/skills/ship/fleet-pull.sh` | keep | Invoked at ship/SKILL.md:41; enumerates members from fleet.json at line 138 rather than hardcoding them, which is why server's 2026-08-01 removal needed no edit here, and every remote op is FF-only with SKIP rows (lines 75-95) — no destructive path. | agents/plugin/skills/ship/SKILL.md:41 |
| `agents/plugin/skills/ship/tests/fleet-pull.test.sh` | keep | Passes (394 lines, the group's largest test); line 221-223 explicitly regression-guards the lost-`</dev/null` swallowed-member bug. Its `server` entry at line 25 is inside a synthetic fixture fleet.json written to $tmp, not the real manifest, so it is a string and not stale state. | nothing found — outside the Justfile:55-56 glob |
| `agents/subagents/research-orchestrator.md` | keep | Not duplicated by agents/plugin/agents/, which ships only quick-tasks.md — different scope on purpose: plugin agents cannot use hooks/mcpServers/permissionMode (claude-code-subagents.md:52), so user-scope subagents live here. Linked entry-by-entry by bootstrap.sh:522 so machine-local agents coexist. Degrades gracefully when gortex-search is absent (does the lookup itself). | agents/bootstrap.sh:522 `link_entries_into subagents -> $CLAUDE_DIR/agents`; documented in agents/docs/claude-code-subagents.md:96-100 |
| `agents/subagents/web-research.md` | keep | 21-line leaf worker; its `disallowedTools: Agent` is the structural enforcement of the no-depth-4 rule that claude-code-subagents.md:60-68 justifies with the documented 380-583-agent fan-out incidents — the constraint would be lost if this were folded into the orchestrator. | agents/bootstrap.sh:522; named as a delegation target by research-orchestrator.md |
| `agents/tests/bootstrap.test.sh` | keep | 289 lines, in the gate, green when run. Coverage gap worth noting rather than a verdict change: it never asserts `install_git_hooks()` (bootstrap.sh:639-658) actually sets core.hooksPath — every reference to hooksPath in the file (lines 154, 156, 175-180) is `-c core.hooksPath=/dev/null` to *suppress* hooks during `worktree add`. | justfile `test` recipe (agents/tests/*.test.sh glob) |
| `agents/tests/orca-profile-harvest.test.sh` | keep | 278 lines, in the gate, green; covers the two non-obvious invariants the script's header argues for — archive semantics (no `--delete`) and accountUuid pairing surviving an Orca re-login. Portability was fixed 2026-08-01 (d8778ca) after the BSD `wc -l` / `/private/var` bugs, so this is one of the suites that turned the gate green. | justfile `test` recipe |
| `agents/tests/orca-profile-link.test.sh` | keep | 191 lines, in the gate, green; same 2026-08-01 macOS-portability fix (d8778ca). Exercises --status / --relink and the live-session skip that lets bootstrap run it unattended after every pull. | justfile `test` recipe |
| `agents/tests/orca-profile-sync.test.sh` | keep | 216 lines, in the gate, green; the only test asserting the settings.json merge path (the one curated entry that is copied rather than symlinked) and the Orca-dir refusal added in 9555ea2 alongside _refresh-claude-config's `env -u CLAUDE_CONFIG_DIR`. | justfile `test` recipe |
| `agents/tests/worktree-dispatcher.test.sh` | keep | 267 lines and 25 assertions covering BOTH dispatchers in one file (setup and teardown), including the two regressions their headers record: the config-path drift that made every gortex guard fail closed, and reconcile reading a list that `gortex untrack` rewrites underneath it. Green, in the gate. | justfile `test` recipe; the only test for worktree-setup.sh and worktree-teardown.sh |
| `agents/worktree-setup.sh` | keep | Live Orca Create-worktree hook, 25 assertions behind it. Header drift only: line 2 says "Two brackets" but the body has three — bracket 1b (lines 116-150) symlinks `.claude/settings.local.json` and `.claude/memory/project.md` from the main checkout and carries a 20-line justification for why `.env` is deliberately excluded, none of which the header mentions. | Orca Create-worktree hook; agents/plugin/skills/orca-setup/SKILL.md + worktree-setup.template.sh; agents/tests/worktree-dispatcher.test.sh |
| `agents/worktree-teardown.sh` | keep | 74 lines, exact reverse of setup (repo-local teardown first, then gortex untrack + reconcile); the reconcile snapshots the tracked list into a variable before looping and the comment at lines 55-59 records the concrete bug that forced it. Green under worktree-dispatcher.test.sh. | Orca Delete-worktree hook; agents/plugin/skills/orca-setup/SKILL.md; agents/tests/worktree-dispatcher.test.sh |
| `docs/2026-08-01-nixos-harvest.md` | keep | Live input, not an archive: §1 is the only written spec for P3's still-unimplemented ssh-server role (tailnet0-only port 22 plus the one hardcoded 192.168.8.0/24 carve-out, not guessable), §2 holds the only surviving copy of the RustDesk peer-ID map, and every claim I could check is still true (tier_fleet_ssh darwin-only, asserted at provision/tests/tiers.test.sh:226). | justfile:5; README.md:11; AGENTS.md:59; provision/gortex.version:7; provision/lib/tiers.sh:685; .claude/memory/project.md:55; docs/fleet-roadmap.md:37,162 |
| `docs/fleet-mesh-history.md` | keep | Despite its own "Dead history" header, lines 138-148 hold the AWG hub peer map that the OPEN roadmap item P6 (lines 482-484, "Drop desktop's AWG… remove once nothing depends on 10.0.0.6, then drop the peer on hub") needs to execute — including that 10.0.0.7 is a friend's device (ilya-romanyuk) that must not be disturbed; and it costs nothing per session since no hook loads docs/ (only .claude/memory/project.md is session-loaded). | .claude/memory/project.md:300,467,528; .claude/kb-harvest-state.json:5,10 (kb-refresh wrote it) |
| `docs/superpowers/plans/2026-07-13-headscale-probe-results.md` | keep | Not a plan but a dated results record, and the only file in this group besides the wsl rollout that already states its status ('**Verdict:** PASS' at line 3) — its node table (latitude 100.64.0.2, homeserver 100.64.0.3) is superseded by today's tailnet but is correctly date-scoped as a 2026-07-13 observation, so it needs no change. | docs/superpowers/plans/2026-07-13-headscale-fleet-mesh-probe.md:370 and :423 (the plan that commissioned it) |
| `docs/superpowers/plans/2026-07-28-latitude-wipe-harvest.md` | keep | The only genuinely LIVE document in this group: the wipe itself completed (§9 'Outcome — installed 2026-07-29', roadmap Done 2026-08-01) but §11 Standing holds are open — the F: ST320LT020 restic-repos/secrets/GoPro/Ubuntu-tar call is explicitly 'deferred, not done', and the whole storage consolidation is blocked on a 6TB replacement after the relabelled-HUS726060ALE611 fraud — and grep for `6TB\|HUS726060\|Kingston\|storage pool` in docs/fleet-roadmap.md returns nothing, so this file is the sole tracker for those holds. | nothing found — no roadmap entry mirrors its open holds |
| `docs/superpowers/plans/2026-08-01-wsl-two-distro-rollout.md` | keep | ABANDONED part-way through Task 4 and already correctly statused — lines 3–27 carry an explicit ABANDONED banner with a per-task executed/never-executed breakdown, the deviation that was undone, and four corrections to its own text; Tasks 1–3 shipped live state (the desktop-wsl rename, fleet.local.json), so it stays as the model header the other 22 plans should copy. | docs/superpowers/plans/2026-08-01-wsl-two-distro-machinery.md:13 (companion plan, another worker's row) |
| `docs/superpowers/specs/2026-07-08-fleet-provisioner-phase3-dotfiles-chezmoi-design.md` | keep | Correctly bannered "SUPERSEDED 2026-07-28 ... Kept for the reasoning, not as guidance" in a blockquote above the Status line — the supersede instruction at plans/2026-07-28-dotfiles-private-bare-repo.md:85 was applied. chezmoi never landed (no dotfiles/ source tree; provision/roles/dotfiles.sh:3 cites the bare-repo spec instead), and this is the deliberate record of why. This banner is the model the rest of the group should copy. | 2026-07-28-dotfiles-private-bare-repo-design.md:5 (Supersedes); plans/…-phase3-dotfiles-executor.md:11 |
| `docs/superpowers/specs/2026-07-08-unified-fleet-provisioner-design.md` | keep | The parent architecture doc, and the ONLY written record of two things the repo still runs on: the §6 role catalog (base / ssh-server / backup-client / agents / dotfiles / repos — literally today's three stubs plus three real executors) and §5's why-JSON-not-TOML rationale for fleet.json. §4 chezmoi, §7 NixOS import generation and §9's AWG recon are dead, but the load-bearing half is not recorded anywhere else. | .claude/memory/project.md:527; docs/fleet-mesh-history.md:168; four child phase specs |
| `docs/superpowers/specs/2026-07-15-fleet-rename-and-magicdns-adoption-design.md` | keep | Shipped and still live — hub / latitude / desktop / server and the gg.ez MagicDNS suffix are today's fleet names. It is the only record of two decisions AGENTS.md states but does not justify: why `.ez` (not a real TLD, `.gg` is a ccTLD, bare `gg` rejected by Headscale) and the explicit reversal of the 2026-07-14 spec's rejection of MagicDNS. Its Layer-1 flake.nix/ssh.nix edits are dead but the doc is a dated migration record. | docs/fleet-mesh-history.md:79; plans/2026-07-15-fleet-rename-layer{1,2-3}*.md |
| `docs/superpowers/specs/2026-07-17-fleet-ssh-tailnet-retire-awg-design.md` | keep | KEEP LOUDLY — this is the original written spec for the ssh-server role, which roadmap P3:310 records as still an unimplemented stub. Its §A gives the exact firewall shape AGENTS.md says only docs/2026-08-01-nixos-harvest.md holds (port 22 on interfaces.tailscale0, plus the verbatim 192.168.8.0/24 extraCommands carve-out, keys-only, the fleet-authorized-keys trust file) — and it adds the rationale the harvest omits: why interface-based beats a source CIDR, and why a rule naming a not-yet-up tunnel iface is safe. | docs/superpowers/plans/2026-07-17-fleet-ssh-tailnet-retire-awg.md:113 |
| `docs/superpowers/specs/2026-07-17-research-orchestrator-design.md` | keep | Shipped, not abandoned: agents/subagents/research-orchestrator.md (opus, use-proactively) and web-research.md (sonnet, leaf) both exist and are non-trivial, and bootstrap.sh links the subagents dir. Its one load-bearing open question resolved in the artifact rather than the spec — web-research.md:5 uses `disallowedTools: Agent`, not the `Task` the spec guessed. Only the claude.nix wiring row is dead. | agents/docs/claude-code-subagents.md:99; plans/2026-07-17-research-orchestrator.md |
| `docs/superpowers/specs/2026-07-17-ssh-wsl-fleet-design.md` | keep | Shipped and load-bearing: provision/ssh-wsl.sh + provision/ssh-wsl.test.sh are live in the `just provision-wsl` chain, the README cites this spec by path, and its §2 tradeoff (private key persisted on /mnt/c under Windows ACLs, accepted) is the written record of a security decision that is not re-derivable from the code. | provision/README.md:367 |
| `docs/superpowers/specs/2026-07-17-tailscale-wsl-self-enroll-design.md` | keep | KEEP LOUDLY — shipped verbatim (provision/tailscale-wsl.sh:126 `--enroll`, :129 `--hostname`, :97 `ts_extract_key_json`, with tailscale-wsl.test.sh covering it) AND it is the only written record of the probed control-server facts you would otherwise re-derive by SSHing hub: headscale v0.29.2 native binary, `preauthkeys create -u` takes a NUMERIC user id not a name, the socket is 0770 headscale:headscale so the mint needs sudo, and user `fleet` = ID 1. | plans/2026-07-17-tailscale-wsl-self-enroll.md:32; specs/2026-07-15-orca-serve-wsl-design.md:6 |
| `docs/superpowers/specs/2026-07-19-fleet-gather-windows-design.md` | keep | Shipped and still cited by live memory: agents/plugin/skills/kb-refresh/fleet-gather.sh implements the platform-dispatch/tar-transport/pushed-distiller design, and project.md points at this spec by path for the detect.hostname identity rule. | .claude/memory/project.md:1159; docs/superpowers/plans/2026-07-19-fleet-gather-windows.md:539 |
| `docs/superpowers/specs/2026-07-19-fleet-hostname-normalization-design.md` | keep | Origin record of the live two-layer naming convention and of the hosts/g16→hosts/desktop and hosts/homeserver→hosts/server renames, which stuck. AGENTS.md now reproduces the convention (including the hub/27608 carve-out), so the unique residue is thin — but the doc is cited from project memory, and its headline change (server.detect.hostname methe-server→g513ie) documents a machine that left fleet.json 2026-08-01, which is worth a dated note rather than a rewrite. | .claude/memory/project.md:258; plans/2026-07-20-fleet-hostname-normalization.md:13 |
| `docs/superpowers/specs/2026-07-20-gortex-bootstrap-wiring-design.md` | keep | Cited from LIVE CODE — agents/bootstrap.sh:531 points at it by path for the binary-vs-wiring split, and the `--no-claude-md` flag it identifies as load-bearing (keeps gortex's rule block out of the git-tracked agents/AGENTS.md) is still the reason bootstrap does not corrupt the shared file. `just gortex-setup` exists. Its NixOS half (pkgs/gortex.nix, development.nix, the me.nix gortex-daemon unit) died with the flake; the pin now lives at provision/gortex.version. | agents/bootstrap.sh:531 (path wraps across the comment line) |
| `docs/superpowers/specs/2026-07-20-ship-fleet-sync-skill-design.md` | keep | Shipped exactly as drawn — agents/plugin/skills/ship/{SKILL.md,fleet-pull.sh,tests/} all exist — and it is the only written statement of the safety invariants of a tool that runs git pull on every fleet box: decision #4 (FF-only, zero destructive remote ops, each failure a SKIP row) and decision #6 (self-detect by tailnet IP, NEVER by OS hostname, because desktop/server map to g614jv/g513ie through fleet.json). SKILL.md states the procedure but not why. | docs/superpowers/plans/2026-07-20-ship-fleet-sync-skill.md; nothing in code cites it by path |
| `docs/superpowers/specs/2026-07-25-hub-fleet-enrollment-tiers-design.md` | keep | Load-bearing: provision/linux.sh:18 cites this spec by path as the explanation for the tier split that is now the whole provisioning architecture, so deleting it orphans a live code comment; only its follow-ups block is stale (it names `just quick`, which no longer exists). | provision/linux.sh:18; docs/superpowers/plans/2026-07-25-hub-fleet-enrollment-tiers.md:13 |
| `docs/superpowers/specs/2026-07-27-macos-one-command-provisioning-design.md` | keep | Cited from live code at provision/provision-mac.sh:6, and shipped verbatim: macos-prep.sh, tailscale-mac.sh, provision-mac.sh, `just provision-mac machine *ARGS` (justfile:89) and provision/tests/provision-mac.test.sh all exist. It carries two rationales the scripts only imply — why there is deliberately no `--authkey` flag (argv is world-readable via ps) and why a tailnet-IP mismatch warns rather than dies (re-running cannot fix Headscale). | provision/provision-mac.sh:6; plans/2026-07-27-fleet-migration-mac-primary-latitude-server.md:303 |
| `docs/superpowers/specs/2026-07-28-dotfiles-private-bare-repo-design.md` | keep | The most load-bearing spec in the group — four shipped scripts carry `# Spec: …` headers citing it down to the section number, so it is the addressed specification those implementations point back to, not a historical record. | provision/dotfiles-sync.sh:3 (§5.1); provision/roles/dotfiles.sh:3 and roles/dotfiles.ps1:3 (§5.4); agents/plugin/hooks/dotfiles-offer.sh:3 (§5.3); .claude/memory/project.md:547 |
| `docs/superpowers/specs/2026-07-29-hus726060ale611-smart-evidence.txt` | keep | KEEP LOUDLY — 324 lines of raw `smartctl -x`, the only copy of unrepeatable primary evidence (read-only capture 2026-07-29T21:20 with the drive seated solely to take the record; the drive is to be returned). Both citing documents call it the attachment: 2026-07-29-storage-pool-hardware-baseline.md:120 and 2026-07-30-6tb-return-claim-ru.md:133, which quote only the ~10 headline fields. Not a spec — primary evidence misfiled under specs/. | specs/2026-07-29-storage-pool-hardware-baseline.md:120; specs/2026-07-30-6tb-return-claim-ru.md:133 |
| `docs/superpowers/specs/2026-07-29-storage-pool-hardware-baseline.md` | keep | Keep loudly — 1242 lines of serials, UUIDs and SMART counters read off live latitude hardware ("do not retype disk paths from memory"), the only written record that the delivered 6TB is not the WD63PURZ that was ordered, and the evidence base for the repo's mount-by-UUID-never-/dev/sdX rule. | docs/superpowers/plans/2026-07-28-latitude-wipe-harvest.md:62,313,648; specs/2026-07-30-g513ie-to-latitude-migration.md:13,438 |
| `docs/superpowers/specs/2026-07-30-6tb-return-claim-ru.md` | keep | An open financial/warranty artifact, not a design: the Russian-language misrepresentation claim demanding refund or replacement for a drive sold as a new WD Purple WD63PURZ that is an 8.5-year-old HGST Ultrastar. Nothing records it as settled — a fixed-string sweep of docs/fleet-roadmap.md, .claude/memory/project.md and docs/superpowers/plans/ finds only the pre-claim finding at plans/2026-07-28-latitude-wipe-harvest.md:635. Deleting an unresolved claim would destroy the user's side of a live dispute. | nothing found outside the SMART dump it cites; unresolved in the roadmap |
| `docs/superpowers/specs/2026-07-30-g513ie-to-latitude-migration.md` | keep | Keep loudly — self-declared "research, incomplete", cited by the open roadmap P2 which leans on its §19.3 telegrind_pgdata duplication finding, and it holds two decisions that must not be re-derived (do not point navidrome at PicardedMusic; restic-server stays off until RESTIC_DATA_PATH is settled). | docs/fleet-roadmap.md:260 (live P2, quotes its §19.3) |
| `docs/superpowers/specs/2026-07-31-pure-repos-to-desktop-design.md` | keep | Status "complete" is accurate, but it is the ONLY tracker of loose ends left open by that migration: 7 Orca-worktree session dirs (~25 MB, 19 transcripts) still held on air awaiting fresh worktree names, and 14 orphaned Docker volumes (323 MB) deliberately not pruned. It also records the `rm -rf` under $HOME vs the dotfiles bare-repo work-tree gotcha and the session-slug path-to-dashes transform. Contains no secret — the Sentry token is noted as living in a transcript, not reproduced. | nothing found — no plan sibling, no code reference |
| `docs/superpowers/specs/2026-08-01-two-distro-orca-design.md` | keep | Keep loudly — it is the only written record of an ABANDON decision (desktop-pure built then `wsl --unregister`ed the same afternoon) and of the four platform surprises that caused it, i.e. exactly the knowledge that stops the idea being retried; note that plans/2026-08-01-wsl-two-distro-rollout.md:928 still instructs an agent to "mark it implemented", which is now wrong. | docs/superpowers/plans/2026-08-01-wsl-two-distro-machinery.md:11; plans/2026-08-01-wsl-two-distro-rollout.md:38,928 |
| `fleet.json` | keep | Accurate against the live fleet: 4 machines with `server` correctly absent (removed 2026-08-01), IPs matching AGENTS.md (latitude .8 / air .7 / desktop .4 / hub .1), and latitude's `profile: "server"` deliberately retained — the profile outlives the machine and provision/tests/tiers.test.sh exercises `plan server`. It is also a live reprovision trigger in scripts/converge.sh's driver gate. | provision/lib/fleet.sh + Fleet.psm1 (manifest readers); scripts/converge.sh driver gate; agents/plugin/skills/lib/fleet-dispatch.sh |
| `hosts/desktop/windows/winget-packages.json` | keep | Standalone, self-describing curated keeper list (valid winget 2.0 schema, CreationDate 2026-07-05) that survives the loss of backup.ps1/restore.ps1 — 1080828 kept it deliberately as "a record of installed apps, useful on its own", and it is consumed by a plain `winget import`, not by any deleted script. | hosts/desktop/windows/windows-reinstall-runbook.md:154 and :215 (the `winget import` restore step); .claude/memory/project.md:1204 |
| `hosts/latitude/debian/archive-mirror.sh` | keep | Live and load-bearing: it is the only second copy of the 663 GiB closed 1970-2024 archive, and its 53-line header records decisions the code cannot show — the exfat suitability checks actually run (no hardlinks, no illegal filenames, 4 files over 4 GiB against exfat's far-higher ceiling), why --partial-dir rather than --append-verify, why the SOURCE dock is the flaky one, and UUID guards at lines 59-60 because sdX letters reshuffle. | hosts/latitude/debian/systemd/archive-mirror.service:11 (ExecStart, under flock); install-timers.sh:41 precondition-checks it |
| `hosts/latitude/debian/install-timers.sh` | keep | Correct and complete: its UNITS array at line 26 lists exactly the four files present in systemd/ and no more (verified against the directory), it precondition-checks both scripts at line 41 before touching systemd, and the header records the two non-obvious calls — why system scope rather than the user scope the three git-sync timers use (rsync -aHAX must reproduce root:root ownership on immich's pg_dumpall output), and why units are copied rather than symlinked (a symlink into the work-tree lets a `git pull` change what root runs on a timer with no review). | AGENTS.md:228, README.md:113, docs/fleet-roadmap.md:114; run by hand (no caller — it is the installer) |
| `hosts/latitude/debian/mirror-refresh.sh` | keep | Live nightly job, and its header is the repo's record of the exit-status bug (lines 40-48: the old last-command `[ -n "$DRY" ] && echo` was falsy under -go, so every successful timer run reported Failed) plus the deliberate exclusions — live PGDATA excluded because an rsync of a running postgres dir is a torn copy, --delete off with the exact --filter/--backup-dir recipe required if it is ever turned on. | hosts/latitude/debian/systemd/mirror-refresh.service:18 (ExecStart, under flock); install-timers.sh:41 precondition-checks it |
| `hosts/latitude/debian/smart-long.sh` | keep | Nothing invokes it — it is absent from install-timers.sh's UNITS (line 26 is the four mirror/archive units only), has no unit or timer of its own, and the sole mention anywhere is prose at docs/2026-07-drive-migration-log.md:330 — but that is by design, not rot: line 6 documents it as a periodic hand-run over a 3-4h budget, and lines 21-22 record that its one former caller (the one-shot overnight.sh migration job) is gone and it now stands alone. Dead-looking but deliberately manual. | nothing found — no unit, no timer, no script caller; only a prose mention at docs/2026-07-drive-migration-log.md:330 |
| `hosts/latitude/debian/systemd/archive-mirror.service` | keep | Live unit with the hardware reasoning inline: shares /var/lock/latitude-mirror.lock with mirror-refresh.service because both read the same usb4/4-2 dock and concurrent reads are what provokes the resets, Nice=10 + best-effort IO to be gentle on the bridge, and TimeoutStartSec=12h justified by the 663 GiB first run across a bridge that drops. | hosts/latitude/debian/install-timers.sh:26 (UNITS array — installed and copied to /etc/systemd/system) |
| `hosts/latitude/debian/systemd/archive-mirror.timer` | keep | Live, and the schedule choice is argued rather than arbitrary: monthly not daily because 1970-2024 is a CLOSED set so a run is almost always a no-op that still costs a 20k-file scan across the flaky bridge, 05:00 on the 1st to clear mirror-refresh at 03:30, Persistent=true to catch up after downtime. | hosts/latitude/debian/install-timers.sh:26 (UNITS) and :27 (TIMERS — the one enabled/started) |
| `hosts/latitude/debian/systemd/mirror-refresh.service` | keep | Live unit; the two ConditionPathIsMountPoint lines make an unplugged dock a clean skip rather than a false alert (fstab's nofail only applies at boot), and the User=root comment records the same ownership reasoning install-timers.sh's header carries. | hosts/latitude/debian/install-timers.sh:26 (UNITS array) |
| `hosts/latitude/debian/systemd/mirror-refresh.timer` | keep | Live, 14 lines, every one justified: OnCalendar 03:30 sits after immich's own 02:00 pg_dumpall so each run picks up that night's dump rather than the previous day's, with Persistent + RandomizedDelaySec=15m so a catch-up run does not fire the instant the box wakes. | hosts/latitude/debian/install-timers.sh:26 (UNITS) and :27 (TIMERS — the one enabled/started) |
| `install-media/autounattend.xml` | keep | 804 lines but not bloat — it is generator output whose exact schneegans config URL is embedded as a comment at line 3, so it is regenerable rather than hand-maintained; verified no secrets (the only <Key> is the public generic Win11-Pro edition selector VK7JG-NPHTM-C97JM-9MPGT-3V66T, ComputerName is TEMPNAME with a Read-Host prompt, account and Wi-Fi are interactive), which also confirms install-media/README.md's "generic, prompts for the computer name" claim. | install-media/ventoy.json's `template: /unattend/autounattend.xml`; install-media/README.md deploy table (line ~11) and Copy-Item block; windows-reinstall-runbook.md:119 and :131; no code reads it |
| `install-media/ventoy.json` | keep | One line of valid JSON that is the entire Ventoy Auto Install mapping (Win11 ISO → the answer file); tracking it is what stopped it being transcribed from runbook prose, and it is the deploy source for P:\ventoy\ventoy.json. | install-media/README.md line ~12 (deploy table) and its Copy-Item block; windows-reinstall-runbook.md:127 transcribes its content inline; no code reads it |
| `provision/.gitattributes` | keep | `* text eol=lf` (line 4) is strictly broader than the root .gitattributes, which pins only `*.sh` — this file is what keeps the .ps1 executors and the extensionless assets/wslopen LF; its line-3 certification 'Everything here is text; no binaries' is the precondition that lets the pattern be `*` here and not at the repo root. | git checkout/attr machinery only; no script reads it |
| `provision/assets/wslopen` | keep | 37-line xdg-open stand-in installed verbatim by wsl-fixes.sh:124 with xdg-open/wslview symlinked to it at :133; Ubuntu 26.04 dropped wslu so nothing else provides it, and without it `claude auth login` hangs the full 180s timeout. | provision/wsl-fixes.sh:124,128,133 |
| `provision/dotfiles-sync.ps1` | keep | Registered as the 10-min `dotfiles-sync` Scheduled Task by windows.ps1:421-437; a faithful mirror of the .sh twin including the settle-debounce (60-108) and the merge-tree preflight (141-152) that keeps <<<<<<< markers out of a live $HOME. Untested — see group notes. | provision/windows.ps1:421 |
| `provision/dotfiles-sync.sh` | keep | The timer body installed by tier_dotfiles_sync (lib/tiers.sh:1336); the merge-tree preflight at sync_merge:208 is the whole safety property (lines 10-14), and it is covered by the 363-line provision/dotfiles-sync.test.sh inside the `just test` glob. | provision/lib/tiers.sh:1336; provision/dotfiles-sync.test.sh:10 |
| `provision/dotfiles-sync.test.sh` | keep | 363 lines / 17 numbered cases against the live provision/dotfiles-sync.sh (12.6KB); builds a real bare repo + temp $HOME and asserts on disk state, not exit status; ran green (ALL PASS) 2026-08-03. | just test — justfile `test` recipe glob `provision/*.test.sh` |
| `provision/fleet-authorized-keys` | keep | Four public-key lines — latitude, desktop-wsl, desktop, air — and that matches the live fleet: methe@server went with the 2026-08-01 decommission, and hub's absence is correct rather than a gap, because hub's tier list (linux.sh:76-78) carries ssh_trust but neither ssh_accounts nor fleet_ssh, so hub is only ever an SSH target, never a client. | lib/tiers.sh tier_ssh_trust:1392; ssh-wsl.sh:318,331; windows.ps1 step 6d:237; provision-mac.sh:97 |
| `provision/fleet-local.sh` | keep | 37 lines, step 4 of the WSL chain (provision-wsl.sh:57-64) and the only writer of the gitignored fleet.local.json that the Windows parent's `wsl -l` discovery reads; covered by provision/tests/fleet-local.test.sh. | provision/provision-wsl.sh:33,57; AGENTS.md:193 |
| `provision/fleet-selfpull.sh` | keep | Installed by tier_selfpull (lib/tiers.sh:1174) and covered by fleet-selfpull.test.sh; the meaningful-exit-status design (10-15) is the recorded fix for latitude silently sitting 23 commits behind. Stale only in the header: lines 5-8 cite the NixOS machines-converge.path and `modules/system/git-autofetch`, both deleted 2026-08-01. | provision/lib/tiers.sh:1174; provision/fleet-selfpull.test.sh:6 |
| `provision/gortex.version` | keep | 17-line data file holding one semver (0.61.4); read by tier_gortex and by scripts/converge.sh's _touches_driver reprovision gate, written by scripts/update-gortex.sh — its own header documents the 2026-08-01 rehoming from the deleted pkgs/gortex.nix. | lib/tiers.sh tier_gortex; scripts/converge.sh _touches_driver; scripts/update-gortex.sh; justfile:161 |
| `provision/lib/fleet-ssh-config.ps1` | keep | Live: windows.ps1:305-325 dot-sources it and calls all three functions; two pure functions plus a -SelfTest run by provision/tests/fleet-ssh-config-ps.test.sh (green, though the -SelfTest arm SKIPs on air with no pwsh on PATH), and lines 19-26 justify the two deliberate divergences from ssh-wsl.sh's jq renderer. | provision/windows.ps1:305,320,321,325; provision/tests/fleet-ssh-config-ps.test.sh |
| `provision/lib/fleet.sh` | keep | 130 lines, every one of its 10 helpers reached: provision.sh:8 sources it (fleet_detect/fleet_machines/fleet_has_machine/fleet_platform/fleet_roles), linux.sh:56 and macos.sh:63 use fleet_profile_for_host, tiers.sh:1251 and roles/dotfiles.sh:31 use fleet_logical_name; fleet-profile.test.sh covers all of them including the retired-'server' rejection at :77. | provision/provision.sh:8; provision/linux.sh:56; provision/macos.sh:63; provision/lib/tiers.sh:1251; provision/roles/dotfiles.sh:31; provision/tests/fleet-profile.test.sh:6 |
| `provision/macos-prep.sh` | keep | 151 lines, stage 1 of the Mac chain (provision-mac.sh:73) and the deliberate single home of every sudo in that chain (7-17), with the three-way scutil hostname trap written up at 92-95; its pure helpers run under MACOS_PREP_LIB_ONLY=1 in provision/tests/provision-mac.test.sh. | provision/provision-mac.sh:55,73; provision/tests/provision-mac.test.sh:2 |
| `provision/provision-mac.sh` | keep | 102-line orchestrator behind `just provision-mac` (justfile:90); it validates the machine against fleet.json BEFORE stage 1 renames the Mac (41-52), an ordering provision/tests/provision-mac.test.sh:25 asserts explicitly. | justfile:90; provision/tests/provision-mac.test.sh:24-25 |
| `provision/provision-wsl.sh` | keep | 77-line orchestrator behind `just provision-wsl` (justfile:98); provision_wsl_steps' ordering and the --no-tailscale ⇒ dispatch=parent rule are asserted at provision/tests/provision-wsl.test.sh:18,24,45, and this file's unbraced-var bug was the real defect that suite had been reporting for weeks. | justfile:98; provision/tests/provision-wsl.test.sh:14 |
| `provision/provision.sh` | keep | The role front door: justfile:80 and provision-mac.sh:84 both call it, the unknown-machine guard at 43-47 is a deliberate anti-'provisioned nothing, reported success' fix, and lines 83-88 are the actual source of the repo's 'not yet implemented (skipped)' message. Only line 2's '(WSL / Linux / nixos)' is stale. | justfile:80; provision/provision-mac.sh:58,84 |
| `provision/repos.sh` | keep | 123 lines wrapped UNCHANGED by both role executors (roles/repos.sh:18, roles/repos.ps1:21) and documented at root README:50; the fzf/DRY_RUN gating at 81-93 is what stops a non-TTY run cloning anything. Note its REPO_GROUPS (25-29) has no `exactly` group even though fleet-selfpull.sh:26 scans $HOME/exactly. | provision/roles/repos.sh:18; provision/roles/repos.ps1:21; root README.md:50 |
| `provision/roles/dotfiles.ps1` | keep | 93 lines of current, correct logic — the guarded checkout at 60-85 and the `remote.origin.fetch` fix at 52 are both real bare-clone traps — and it correctly defers timer registration to windows.ps1 (8-9). No stale NixOS content, unlike its agents/repos siblings. Reachable only by hand via provision.ps1. | dot-sourced by provision/provision.ps1:13-14,20 |
| `provision/roles/dotfiles.sh` | keep | 253 lines and the most load-bearing role executor: _dotfiles_collisions/_dotfiles_backup_collisions, the key-material refusal at 44, the set-upstream fix at 220-222 recorded from a live latitude failure 2026-07-29, and the tier_dotfiles_sync sourcing at 236-252. Only the 12 lines of nixos/home-manager reasoning at 9-20 and the `nixos\|` token at 116 describe a platform that no longer exists. | sourced by provision/provision.sh:11-15; provision/tests/dotfiles-collisions.test.sh; provision/tests/roles.test.sh:27,30,50 |
| `provision/roles/repos.ps1` | keep | 38-line wrapper running provision/repos.sh under Git Bash; no stale content. Same reachability caveat as its dispatcher, and lines 23-28 are the Git-Bash-location block duplicated verbatim from agents.ps1:26-31. | dot-sourced by provision/provision.ps1:13-14,21 |
| `provision/roles/repos.sh` | keep | 42 lines that behave identically on every posix platform; unlike agents.sh its `nixos\|` at line 21 is a dead token in a case list sharing one body, so nothing misbehaves — only the 6 lines of nixos-vs-home-manager justification at 6-8 and 22-24 argue about a machine that is gone. | sourced by provision/provision.sh:11-15; provision/tests/roles.test.sh:27,31,35 |
| `provision/ssh-wsl.test.sh` | keep | 221 lines covering ssh-wsl.sh's pure helpers via SSH_WSL_LIB_ONLY; pins CONFIG_MARKER_BEGIN verbatim (line 17), which tiers.test.sh:283 cross-checks so the two ~/.ssh/config writers cannot collide; green. | just test — justfile glob `provision/*.test.sh`; its marker constant is re-asserted by provision/tests/tiers.test.sh |
| `provision/statusboard/disks.latitude5520.conf` | keep | Read at runtime by statusboard.sh:2019 (SB_DISKMAP defaults to disks.$(hostname).conf); 56 lines of which 46 are the 2026-08-01 hardware survey that makes the 7 directives auditable, and it encodes the two facts the board cannot derive — physical bay labels and /mnt/xs being transient rather than a fault. | provision/statusboard/statusboard.sh:2019 |
| `provision/statusboard/statusboard-gui.sh` | keep | 749 lines / 15 functions with a real reason to exist stated at :7-24 (a psf console font carries only 3 of the 8 U+2581-2588 ramp glyphs, so the VT board falls back to an ASCII ramp) and :26-40 (why an autologin getty seat rather than a system unit); covered by provision/tests/statusboard-gui.test.sh, green. | provision/tests/statusboard-gui.test.sh:19; provision/lib/tiers.sh:212,221 (tier_statusboard installs its cage/foot/fontconfig prerequisites); invoked only by a deliberate `sudo bash statusboard-gui.sh --install` — nothing in the repo runs it automatically |
| `provision/statusboard/statusboard.sh` | keep | 144 KB / 2992 lines / 90 functions / 18 labeled sections, and the size is justified rather than bloat: it is deliberately dependency-free (:44-48 — bash, coreutils, /sys, /proc only, no python/curl in the hot loop), the long comment blocks each record a measured failure rather than prose (e.g. :2965-2985 diagnoses the tmux blink down to a 3708-byte frame and an erase-then-draw write(2) race), and provision/tests/statusboard.test.sh sources it to exercise the pure helpers with fixtures plus runs `--once` end to end — green. | provision/tests/statusboard.test.sh:18; provision/statusboard/statusboard-gui.sh:137 (BOARD=…/statusboard.sh); run only via a deliberate `sudo bash statusboard.sh --install` (tier_statusboard is packages-only, asserted by tiers.test.sh:84-85) |
| `provision/tailscale-mac.sh` | keep | 163 lines, stage 2 of provision-mac.sh:77; the deliberate absence of an --authkey flag (13-16: argv is world-readable through `ps`) and the manifest-IP verification at 143+ are both real decisions, and the tsm_* helpers are unit-tested under TAILSCALE_MAC_LIB_ONLY=1. | provision/provision-mac.sh:56,77; provision/tests/provision-mac.test.sh |
| `provision/tailscale-wsl.sh` | keep | 274 lines, step 1 of provision-wsl.sh:56, carrying the one-tailscaled-per-WSL2-VM finding proven live 2026-08-01 at lines 8-18 plus an explicit retraction of the earlier false claim; ts_* helpers covered by provision/tailscale-wsl.test.sh:11. | provision/provision-wsl.sh:30,56; provision/tailscale-wsl.test.sh:11 |
| `provision/tailscale-wsl.test.sh` | keep | Smallest suite at 40 lines and thin relative to its 14.3KB subject (only ts_sanitize_hostname / ts_pick_key / ts_extract_key_json), but every assertion targets live functions and the authkey-precedence coverage is the part that silently mis-enrolls a distro; green. | just test — justfile glob `provision/*.test.sh`; step order asserted separately by provision/tests/provision-wsl.test.sh |
| `provision/tests/dotfiles-collisions.test.sh` | keep | Sources the real provision/roles/dotfiles.sh and builds a genuine bare repo; the premise assertion at 'premise: git refuses the checkout while the collision stands' means the suite self-retires if git ever stops refusing — that is the rare test that knows when it becomes dead weight. | just test — justfile glob `provision/tests/*.test.sh`; subject is provision/roles/dotfiles.sh |
| `provision/tests/expansion-multibyte.test.sh` | keep | Repo-wide byte-level scan for `"$var<multibyte>"` with a positive control (bait file must HIT), a negative control (braced form must not fire) and a live locale probe — this is the class of bug that shipped in provision.sh's --apply path; green. | just test — justfile glob `provision/tests/*.test.sh`; scans every *.sh in the repo, so it is the only cross-cutting suite |
| `provision/tests/fleet-local.test.sh` | keep | Exercises the real provision/fleet-local.sh including the 2026-08-01 .self.dispatch field: default `direct`, explicit `parent`, and rejection of a typo'd mode — the failure it guards makes a WSL distro unreachable; green. | just test — justfile glob `provision/tests/*.test.sh`; subject invoked as step 4 of just provision-wsl |
| `provision/tests/fleet-logical-name.test.sh` | keep | 53 lines guarding fleet_logical_name, whose wrong answer puts a machine's commits on another machine's dotfiles branch; case 3 specifically forbids resolving to the empty string (would check out branch ''); green. | just test — justfile glob `provision/tests/*.test.sh`; subject is provision/lib/fleet.sh |
| `provision/tests/fleet-profile.test.sh` | keep | Already updated for the 2026-08-01 world: line 22 asserts latitude's `server` PROFILE survives, and lines 77-78 assert fleet_has_machine REJECTS the retired `server` machine — plus the jq-free python3 fallback path hub depends on; green. | just test — justfile glob `provision/tests/*.test.sh`; also drives provision/provision.sh end-to-end for the exit-2 unknown-machine guard |
| `provision/tests/fleet-ssh-config-ps.test.sh` | keep | The only suite covering any PowerShell in the repo, and the one that demonstrates the workable pattern (-SelfTest plus platform-independent BOM/CRLF/parity greps); its cross-renderer parity check against ssh-wsl.sh is real coverage even where the -SelfTest half SKIPs. | just test — justfile glob `provision/tests/*.test.sh`; subject is provision/lib/fleet-ssh-config.ps1 |
| `provision/tests/fleet-ssh-tier.test.sh` | keep | Runs the real tier_fleet_ssh against a throwaway $HOME and DERIVES its per-member assertions from fleet.json (`jq -r '.machines \| keys[]'`, and n_want = machine count + 1) — the comment at line 51 records that a hardcoded five-member list is exactly what the server removal broke; green. | just test — justfile glob `provision/tests/*.test.sh`; subject is tier_fleet_ssh in provision/lib/tiers.sh |
| `provision/tests/git-autofetch.test.sh` | keep | Behavioural rather than grep-based: extracts the generated script from tiers.sh's heredoc and asserts refs actually MOVED, that a masked timeout(1) still bounds a hang, and that shallow clones are skipped without tripping the all-failed exit; the awk extraction is coupled to the heredoc delimiter but line 31 fails loudly (`[ -s "$AF" ] \|\| exit 1`) rather than silently passing. | just test — justfile glob `provision/tests/*.test.sh`; subject is the AUTOFETCH heredoc in provision/lib/tiers.sh tier_autofetch |
| `provision/tests/provision-wsl.test.sh` | keep | This is the suite whose red was reporting a real shipped bug for weeks (the unbraced `"$var…"` fatal under set -u), and it now also proves --no-tailscale implies `--dispatch parent` by stubbing every step script and reading the invocation log rather than grepping; green. | just test — justfile glob `provision/tests/*.test.sh`; subject is provision/provision-wsl.sh and the five-step chain it names |
| `provision/tests/ssh-accounts.test.sh` | keep | Runs the real tier_ssh_accounts against a throwaway $HOME with `env -u XDG_CONFIG_HOME` so the git --global write cannot escape the sandbox; both failure modes it guards (a key authenticating as the wrong account, an includeIf keyed on a renamed alias) are silent, which is what earns the 129 lines; green. | just test — justfile glob `provision/tests/*.test.sh`; subject is tier_ssh_accounts in provision/lib/tiers.sh |
| `provision/tests/statusboard-gui.test.sh` | keep | 436 lines against the live 36KB provision/statusboard/statusboard-gui.sh; the two invariants it pins (cage -s, and the hook refusing to exec) are both unrecoverable-from-the-box failures per its header, so a pure-function test is the only safe way to cover them; green. | just test — justfile glob `provision/tests/*.test.sh`; subject is provision/statusboard/statusboard-gui.sh, cross-checked against statusboard.sh at line 337 |
| `provision/tests/statusboard.test.sh` | keep | 94KB is the biggest test in the repo but it is 1329 lines at ~71 bytes/line with 465 assertions against a 147KB subject — proportionate, not bloated; every assertion sources the real statusboard.sh under STATUSBOARD_LIB_ONLY and the `server` strings at lines 856/1004 are synthetic sb_fleet_parse input, not manifest lookups; green. | just test — justfile glob `provision/tests/*.test.sh`; subject is provision/statusboard/statusboard.sh |
| `provision/tests/sudo-nopasswd.test.sh` | keep | Drives the real tier_sudo_nopasswd with a stub visudo whose verdict the test flips, and asserts that a rejection installs NO file — a broken /etc/sudoers.d drop-in makes sudo refuse to run at all, recoverable only at the physical console on a headless box; also pins mode 0440 and idempotence; green. | just test — justfile glob `provision/tests/*.test.sh`; subject is tier_sudo_nopasswd, the server-profile-only first tier (tiers.test.sh:63-65) |
| `provision/tests/wsl-fixes.test.sh` | keep | Covers the binfmt watchdog and the diverted native docker CLI, and its numeric assertions are calibrated to a measurement (OnUnitActiveSec <= 15s, sized from seven unregisters in 25 minutes on 2026-08-01) rather than a guess; lines 27-30 deliberately restore pass/die after sourcing because wsl-fixes.sh's own die() calls exit 1 and would have hidden every later assertion; green. | just test — justfile glob `provision/tests/*.test.sh`; subjects are provision/wsl-fixes.sh and provision/assets/wslopen |
| `provision/windows.ps1` | keep | 445 lines and the real Windows entry point (root README:44, justfile:18,104): seven steps ending in the two Scheduled Tasks nothing else registers (fleet-selfpull:395, dotfiles-sync:421) plus the entire OpenSSH-server story at 210-350 that the never-implemented `ssh-server` role would otherwise own. Only line 278's 'the NixOS spokes' is stale. | root README.md:44; justfile:18,104; AGENTS.md:176 |
| `provision/wsl-fixes.sh` | keep | 209 lines, step 5 of provision-wsl.sh, covered by provision/tests/wsl-fixes.test.sh:34; the binfmt finding at 16-28 (a graceful shutdown of ANOTHER distro unregisters WSLInterop VM-wide, and the obvious binfmt.d fix is inert) is not re-derivable from the code. One doc bug: line 9 says 'Two fixes' then lists three at 11, 16 and 30. | provision/provision-wsl.sh:34; provision/tests/wsl-fixes.test.sh:34 |
| `scripts/converge.sh` | keep | The fleet's fourth git-sync mechanism and the only one that APPLIES a pull rather than fetching it; 193 lines, every gate commented with the bug it prevents — the darwin arm added 2026-08-01 (air had silently run the apt driver since joining), fleet.json moved into _touches_driver at line 107 after the touches_nix deletion exposed that it had been a reprovision trigger on no box at all, and the nixos arm at line 147 deliberately refuses and records a FAILURE rather than advancing converged-rev. Verified green: `bash scripts/converge.test.sh` → ALL PASS. | agents/git-hooks/post-merge job 2 — backgrounds it via setsid/nohup on POSIX, or fires `schtasks /run /tn machines-converge` on Windows; that SYSTEM task is registered by provision/windows.ps1:352; provision/fleet-selfpull.{sh,ps1} reach it indirectly because their pull fires the same post-merge hook; justfile:163 names it |
| `scripts/converge.test.sh` | keep | Ran it this session: ALL PASS, and it is in `just test`'s globs (justfile:56 `scripts/*.test.sh`). Not decorative coverage — it pins the two 2026-08-01 regressions in place (line 43-44 assert a stray .nix must NOT fire either gate, so a regrown Nix dependency shows up here; lines 100-103 assert fleet.json fires on BOTH tiers so it cannot be re-narrowed to one) and it covers box_class, which was previously untested and is how Darwin stayed in the linux catch-all. | justfile:55-56 (`just test` glob `scripts/*.test.sh`) |
| `scripts/update-gortex.sh` | keep | Wired end to end and consistent with its two readers: it writes provision/gortex.version, whose reader contract (first bare semver on a non-comment line, no `v` prefix) the awk at lines 40-44 respects, and its `repo="zzet/gortex"` matches the download URL tier_gortex actually uses at provision/lib/tiers.sh:710. The header records that it was Nix-coupled and unrunnable fleet-wide until 2026-08-01, and lines 48-49 correctly tell the user the bump only lands once committed, because the pin is a _touches_driver trigger. | justfile:167-169 (`just update-gortex`); provision/tests/justfile.test.sh:79 asserts the recipe exists; provision/gortex.version's header names it as the writer |

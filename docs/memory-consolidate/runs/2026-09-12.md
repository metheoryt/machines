# memory-harvest Phase B — 2026-09-12, g15

## FIRST LINE, as Step 8 requires: the repo-side invariant diff failed, then resolved itself

**`/home/me/machines/.claude/memory/project.md` was clean at Step 0 and was dirty
at the Step 8 check**, along with `AGENTS.md`, `provision/orca-serve.sh` and
`provision/orca-serve.test.sh`. Nothing was reverted, per Step 8.

**Then it resolved while the report was being written**: the other session
committed its own work as `178b5d7 orca: a desktop install must launch the
AppImage, not an unpacked tree`, and a re-run of the Step 8 check against the
same Step 0 baseline is now **empty on both sides**. That is why this run does
commit after all. The sequence is recorded rather than tidied away, because the
window in which a correct run reports its own invariant as violated is the
finding.

**It was not this run, and the evidence is checkable.** The changes are one
coherent piece of feature work on Orca's AppImage self-update — a 234-line change
to `provision/orca-serve.sh`, 36 lines of new assertions in its test, a 10-line
`AGENTS.md` paragraph, and a Lane-1 `project.md` section
`## Orca on g15 never self-updates: an EXTRACTED AppImage cannot (2026-09-12)`.
Phase B neither did that nor was tasked with it; all nine pass-1 subagents were
forbidden to write and each reported zero writes. Timestamps 04:06:39–04:11:40,
inside this run. And `ps` shows **three concurrent
`claude --dangerously-skip-permissions` sessions with transcripts under
`~/.claude/projects/-home-me-machines/`** — `96a76e02` (this run), `1abc7229` and
`08aa9fe8`, the last two still being written at 04:16.

So this is a **concurrent edit by another session in the same working tree**, not
a Phase B invariant violation — a false-positive mode Step 8 does not anticipate
(it excuses only the `dotfiles-sync` merge, which commits and therefore leaves
the tree clean). Filed as `effa9c8c`. **A human should still look**, because two
sessions were writing `machines` at once tonight and only one of them knew it.

The **dotfiles-side diff IS empty**: no `$HOME` memory store, no `core.md`,
`global.md`, `personality/*` or `host-memory.md` was touched. Everything Phase B
wrote is under `docs/memory-consolidate/`.

**The output is also backed up** to `…/scratchpad/phaseB-backup/` (`queue.md`,
this report, `replacements/`) — taken while the commit was still withheld and
63 items lived in an uncommitted tracked file that the concurrent session's
`git add -A` could have absorbed. Kept as the fallback.

Run on **g15**, the `memory_publisher` (`fleet_memory_publisher` =
`fleet_logical_name` = g15), from the main `~/machines` checkout, clean on
`main`. Phase A of the same night finished minutes earlier on this box.

---

## Scanned

**20 stores, 761,117 B** — but **45,954 B of that is a double-count**: see the
worktree finding below. The honest store corpus is **18 stores, 715,163 B**.

| store | bytes | scope |
|---|---|---|
| `machines/.claude/memory/project.md` | 286161 | repo:machines |
| `~/.claude/memory/global.md` | 122120 | shared |
| `my/vps/.claude/memory/project.md` | 55152 | repo:vps |
| `my/skep/.claude/memory/project.md` | 54879 | repo:skep |
| `my/qaz-code/.claude/memory/project.md` | 41639 | repo:qaz-code |
| `~/.claude/memory/personality/practices.md` | 32725 | shared |
| `my/qaz-pool/.claude/memory/project.md` | 23571 | *worktree of qaz-code — not a store* |
| `qaz-baseline/.claude/memory/project.md` | 22383 | *worktree of qaz-code — not a store* |
| `my/embedthat/…/project.md` | 21369 | repo:embedthat |
| `my/buton/…/project.md` | 15915 | repo:buton |
| `my/DeMarket/…/project.md` | 14663 | repo:DeMarket |
| `my/telegrind/…/project.md` | 11336 | repo:telegrind |
| `~/.claude/memory/personality/tone.md` | 11307 | shared |
| `~/.claude/host-memory.md` | 10091 | host |
| `~/.claude/memory/personality/values.md` | 7034 | shared |
| `my/vasya/…/project.md` | 6630 | repo:vasya |
| `my/airdrome/…/project.md` | 6147 | repo:airdrome |
| `~/.claude/memory/personality/habits.md` | 5875 | shared |
| `my/housing/…/project.md` | 5349 | repo:housing |
| `~/.claude/memory/core.md` | 3330 | shared |

**Untracked stores: none.** Every store is `shared`, `host`, or `repo:<name>`.

**Instruction files: 17, 360,606 B** — again minus **119,577 B** of worktree
double-count, so **15 files, 241,029 B**. Largest: `my/qaz-code/CLAUDE.md`
64852, `machines/AGENTS.md` 52638, `my/telegrind/CLAUDE.md` 13895.

**The corpus grew ~30% since 2026-09-11** (574 KB → 715 KB of stores) because six
repos gained their first `project.md` in tonight's Phase A. The old "two stores
hold 70%" split is still roughly right: `machines/project.md` + `global.md` =
**57%** of the store corpus.

## Items

**63 filed, 0 suppressed.** Not one candidate collided with an open or decided
id — which is the first clean result for the suppression design, and the reason
is in the `skill` items below: the discriminator that makes siblings distinct
finally got passed (through a workaround), so nothing merged into a `### Part N`
blob this run.

| action | filed tonight |
|---|---|
| contradiction | 14 |
| dedupe | 10 |
| delete | 9 |
| compress | 9 |
| skill | 8 |
| promote | 5 |
| demote | 5 |
| generalise | 3 |
| **total** | **63** |

Queue: **10 open before → 73 open now**, 178 KB. (Counted mechanically off
`first seen:` per item, not by subtracting whole-queue totals — the whole-queue
`demote` count is 13 because 8 of them are the pre-existing `Pure …` cluster,
and reporting that as tonight's number would have been exactly the kind of stale
figure `AGENTS.md` warns about three separate times.)

**Two items supersede two of my own**, filed minutes earlier in this same run
with invented anchors (`94fc81c8` → `fe27f639`; `1939af4c` → `03b60232`). An
item is never rewritten in place, so the correction arrives as a second item;
`/memory-review` should reject the two originals as mis-anchored duplicates.
Cause: I typed an anchor from memory instead of copying `index`/`instructions`
output. `AGENTS.md` has only four `##` headings and `vps/project.md`'s is
`Services & scheduler`, not the bolded inline labels I used. Every other anchor
this run was verified against the tool before filing.

## Bytes, now → if every open item were applied

Per-item numbers are in the items. Summed over the 63 filed tonight, the net is
roughly **−28 KB across the corpus** — but that figure is close to meaningless
on its own and should not be quoted as the value of the run:

- Eight of the highest-value items are **byte-neutral or byte-positive**: a
  correct pointer costs more than a wrong one (`b069cc28`, the dead restic
  restore path, is 437 → 471), and a contradiction marker costs ~600 B to save a
  wrong hardware purchase (`f6c079f9`).
- The one large reduction is `01ba3c32`, −3819 B, the 12.6 KB WSL-migration
  narrative for a machine state that no longer exists.
- The **structural** items (`63d81ceb`, `b2014b67`) move ~12.5 KB without
  deleting a byte, and are worth more than any of the deletions.
- Measured negative, recorded so nobody re-opens it: `my/qaz-code/CLAUDE.md` is
  **not meaningfully reducible** — both items against it save 738 B of 64,852
  (1.1%), 74% of the file is `## Architecture`, and **zero** non-blank lines over
  40 chars repeat anywhere in it. There is no duplication to remove.

## Standing checks

- **`core.md`: 3330 B on disk, 2608 B injected.** Well inside the ~3.4 KB harness
  stdout cap (~800 B headroom); over the ~2 KB style budget on file bytes. **Both
  numbers reported together, deliberately** — and item `9b955990` fixes the place
  in `~/.claude/CLAUDE.md` that derives one from the other causally, which is how
  the 2026-09-11 run filed a false near-truncation alarm.
- **Transcript gap: 1 of 44 local sessions, and that one is this session.** The
  brief's own recipe said 29 of 44; it compares every project slug on the box
  against **one** repo's state file, and `.sessions` also carries other boxes'
  sessions merged in by `fleet-gather.sh`. Per repo, against its own state file:
  machines 1 (open session), airdrome 0, embedthat 0, qaz-code 0, telegrind 0,
  vps 0. **Open item `0c1fdb6a` rests on the miscount and its premise is false**
  — flagged in `2249f890` rather than rewritten, since a run may not edit an
  existing item. Limit worth stating: from g15 only g15's transcripts are
  visible; `buton` has 180 harvested sessions and no local transcripts dir, so
  fleet-wide coverage cannot be asserted from here.
- **`verify`: 87 ok, 4 drifted, 0 missing.** All four "drifted" rows are the
  2026-09-11 `dream`→`memory-harvest` / `dream-apply`→`memory-review` **rename**,
  not drift: each phrase was re-checked at the new path with `grep -cF` and all
  four were found. `verify` cannot follow a rename because the ledger freezes the
  target path at decide time — filed as `5d2bfaaa`.
- **Untracked store: none.**
- **A store nobody reads: none.** Every one of the 18 stores was modified within
  the last two days (16 today, 5 yesterday).
- **Rule→hook escalation: checked, nothing filed.** The brief's live example is
  the gortex `remember` instruction with an empty store. The instruction appears
  once in `~/.gortex/instructions/active.md` and is not restated in three stores,
  and `sqlite3` is not installed on this box so `~/.gortex/memories/sidecar.sqlite`
  could not be read. **Insufficient evidence, so no item** — the `hook` action
  needs a restated rule AND a measured non-compliance, and I have neither.
- **Ledger categories rejected 3+ times:** the only such class is
  "promote a branch's shared-store drift" (`6d32518b`, `08ade540`, `6b30ae15`,
  all rejected 2026-09-11 as *already done*). This run files three anyway
  (`d0971e33`, `5fb4bf31`, `a82195c2`) — **not a fourth attempt at the same
  thing**: those three were rejected because a byte/size comparison produced
  false positives, and these are derived from a merge-base line diff, a different
  and stricter test. The rule change that makes that the standard is `a216a4c2`.

## Fleet pass (Step 3b) — the biggest finding of the run

`hub` and `latitude` are byte-identical to `origin/main` on all eight shared
paths. Three branches are not, and **size comparison would have missed the
largest one entirely**:

| branch | merge-base | lines on the branch that main has never had |
|---|---|---|
| desktop-wsl | ae03a7d (09-11) | practices.md **126**, tone.md 21, values.md 13, core.md 5, .claude/CLAUDE.md 5 |
| g15 (this box) | 21abe71 (09-11) | global.md **166** (the whole `## Harvested 2026-09-11` section) |
| air | 57551c6 (08-24) | global.md 17, practices.md 11, tone.md 7 |
| desktop | 57551c6 (08-24) | 1 line each in the two UNC-symlink blobs — not a finding |

`desktop-wsl`'s `practices.md` is **735 B SMALLER** than main's and holds 126
lines of unpromoted personality memory. The brief's current Step 3b rule says a
branch smaller than main "is just lagging its next sync tick; that is not a
finding" — measurably wrong, filed as `a216a4c2` with the merge-base recipe that
replaces it. The whole-file `comm` is wrong in the other direction too: it
reports 332 desktop-wsl lines "main lacks", most of them text main *deliberately
deleted* on 2026-09-11 — which is exactly how three items were rejected as
*already done* last run.

**Two of the three promotes must be applied on another box** (`desktop-wsl`,
`air`); the write boundary is unchanged and `/memory-review` here must refuse
them. Only `d0971e33` (g15) is applicable on this box, and it should be applied
**after** the fold `63d81ceb`, or the duplicates go fleet-wide and get promoted
twice.

## What this run could not read, and why

- **`~/pure/backend-api/.claude/memory/project.md` (58333 B) was not read.** It
  is absent on g15 (`repo_groups: ["my"]`) and exists only on
  `origin/desktop-wsl`. The six open `Pure …` demote items depend on it and
  remain blocked on the same `/dotfiles-promote` they were blocked on yesterday.
- **`~/.gortex/memories/sidecar.sqlite`** — no `sqlite3` on this box.
- **The stores created or extended by tonight's Phase A got no pass 1**
  (airdrome, buton, DeMarket, housing, telegrind, vasya, skep, embedthat): they
  are fresh append-only output from a few hours ago and Phase A already filed
  proposals against them. They are first in line next run. **Pass 2 DID cover
  them** — see the measurement below; skipping pass 1 was not a reason to skip
  the cross-store check, and treating it as one was an error caught before
  filing.
- **`machines/AGENTS.md` (52638 B) got no dedicated pass** — it was read only as
  the *survivor* side of project.md dedupes, which produced 2 items against it.
  A dedicated instruction-file pass over it is the other thing next run should do.

## Pass 2 (cross-store) — a clean measured negative

The brief calls cross-store duplication "the actual reason this skill exists",
and the six stores written tonight by eleven independent subagents with no view
of one another are the highest-probability duplication surface in the corpus. So
it was measured rather than assumed, over all **18 real stores** (worktree copies
excluded), every non-blank line of 40+ characters, `LC_ALL=C`:

```
lines of 60+ chars appearing in two or more stores:  1
lines of 40+ chars appearing in two or more stores:  2
```

Both are incidental and neither is worth an item: a provenance comment
(`<!-- src: airdrome adae7fe | 2026-07-29 -->`, in two stores by design) and one
sentence fragment shared by `personality/values.md` and `personality/habits.md`
("(memory/personality/), loaded every session and synced across machines.").

**There is no substantive verbatim duplication between stores.** The cross-store
findings this run did file are all *semantic* rather than verbatim — the same
fact stated differently in `project.md` and `AGENTS.md`, or a fact sitting at the
wrong scope — which is why a line-level sweep returns nothing and the pass-1
agents cross-reading each other's survivors found six. Worth remembering next
run: the line-level sweep is cheap and rules out a whole class in two commands,
but it is not the pass.

## Collision notes for `/memory-review`

- **All 11 Phase A `shared-proposal-2026-09-12.md` files queue additions to
  `global.md`.** 24 of tonight's items also target `global.md`. Batch them into
  one `/dotfiles-promote`, and decide the fold (`63d81ceb`) before promoting
  (`d0971e33`). Measured: the 11 proposals do **not** duplicate each other —
  217 long lines across them, zero repeated 60-char prefixes except the format
  header.
- **Two items overlap open item `8dd3022d`** (`a6f70ff5`, `34ccf2ed`): they are
  *alternatives*, not a sequence, and `8dd3022d` must be applied on `desktop`
  while these can be applied here. Pick one per passage.
- **`01ba3c32` and `e0003b52` are either/or** — the second's range is inside the
  first's and the first already folds the correction in.
- **`/improve config audit` overlaps** this run's 6 instruction-file items
  (`~/.claude/CLAUDE.md` ×2, `~/CLAUDE.md`, `AGENTS.md` ×2, `qaz-code/CLAUDE.md`
  ×3). Say so if both are run the same day.
- Phase A's `machines` proposal records the `--match` basename collision as a
  **host:g15 fact**; this run's `60604a4f` changes the **rule** in Phase A's
  SKILL.md. Complementary, not competing — apply both.

## One convention this run invented

`docs/memory-consolidate/replacements/<id>.md` — a sibling file holding an
oversized replacement block (one file, 8808 B, for `01ba3c32`), so a 12.6 KB
section rewrite does not go inline into the queue. It is inside the phase's write
boundary, but it is **not in the README and `consolidate.sh decide` does not know
about it**: deciding that item cuts it from `queue.md` and orphans the file.
Fine for one file; flagged so the next run neither rediscovers the pattern nor
assumes it is supported.

## Self-findings (`skill`, 8)

1. `d5f4b69f` — **the mandatory four-tuple id cannot be expressed.**
   `consolidate.sh id` hashes three arguments and silently drops a fourth;
   measured, three identical hashes. The 2026-09-11 fix (`f7082e6e`) changed the
   prose and never touched the tool, so the mandate has been unsatisfiable since
   it was accepted. **Tonight's workaround:** the discriminator was folded into
   the *anchor argument* as `<anchor>#<basename>:<a>-<b>`, and every item records
   the literal string passed in an `id-inputs` field so the id is reproducible.
   That convention is not in the brief; the next run will not guess it.
   **This is a deadline, not a preference: `d5f4b69f` must be applied before
   Phase B runs again.** Tomorrow's run reads the unmodified brief, calls
   `id <target> <anchor> <action>` with the bare anchor, gets a different hash,
   `status` returns `new`, and appends — **63 duplicates, one per item filed
   tonight**, with no way to tell the pairs apart afterwards. It also breaks the
   brief's own acceptance test ("run it twice, the second run must file zero new
   items") on the very next run.
2. `5d2bfaaa` — `verify` cannot follow a rename; all four `drifted` rows tonight
   were relocations with content intact.
3. `2249f890` — the Step 5 transcript-gap recipe overstated the gap 29×.
4. `a216a4c2` — Step 3b's size test for branch drift; replaced with merge-base.
5. `b2db3785` — `scan`/`instructions` count a **git worktree's** checkout of a
   tracked file as a separate store. `qaz-baseline` and `qaz-pool` are worktrees
   of `~/my/qaz-code`; one tracked `project.md` is reported three times at three
   sizes, and the same for `CLAUDE.md`. **~165 KB, a quarter of tonight's
   reported corpus, is two stale checkouts** — and the run was actively invited
   to file items against paths whose edits are lost on the next checkout.
6. `60604a4f` — Phase A Step 0: "the repo's basename is enough" is false on this
   box (`~/kazakhstan-law/codes` vs `~/split-test/codes`; `kazakhstan-law` is a
   substring of every nested child's slug).
7. `ae9ea0da` — Phase A Step 0: the recovery backup is taken *after* the
   first-run `{}` create, so restoring it wipes a watermark instead of restoring
   one. Two Phase A subagents hit it independently.

Two further things worth a future `skill` item but **not filed**, because
neither has a run that failed on it yet:
- **A `## Harvested <date>` heading is an ingestion artefact.** Both `global.md`
  and `machines/project.md` now carry one (11.2 KB and 19.3 KB), and folding them
  is two of tonight's items. If the fold is accepted, the rule — *a harvest
  writes into the topical heading its facts belong to* — belongs in Phase A's
  Step 6, not rediscovered here every night.
- **`LC_ALL=C` before any `sort`/`comm`.** This box has a Russian locale;
  `comm` emitted "input is not in sorted order" and returned inflated counts
  twice in this run before it was noticed. It is in the `2249f890` and `a216a4c2`
  replacement snippets, but not as a standing rule.

## Commit and push

**Committed, not pushed.** The Step 8 diffs are empty against the Step 0
baseline (see the first section — the transient failure was another session's
uncommitted work, now committed), so `git add docs/memory-consolidate` +
`git commit` went ahead, scoped to this phase's own directory. The push is ruled
out independently: `git log --oneline origin/main..HEAD` shows **9
pre-existing commits, none `dream:`-prefixed** (`e703b9e`, `7a610ba`, `73a5334`,
`0624418`, `1b38ff8`, `d0bcdda`, `e3bcc54`, `8db945f`, `ac285c4` — Phase A's and
last night's work). — now ten, with the concurrent session's `178b5d7`. An unattended push would
carry all of them to `origin`. Left for an attended session.

**Worth an attended session's attention regardless:** two Claude sessions were
writing `~/machines` at the same time tonight and neither knew about the other.
Nothing was lost, but the collision was visible only because Step 8 happened to
be looking.

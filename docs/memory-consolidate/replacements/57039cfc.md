# Replacement text for queue item `57039cfc` (skill · consolidate-phase.md)

**Why this file exists:** the item's inline `replacement:` block nests a
```` ```bash ```` fence inside a ``` fence. An indented bare ``` is a valid
closing fence, so the outer block terminates early and a reviewer copying it out
gets only the first eight lines. The item is never rewritten in place (`Never`
list), so the complete text lives here. The convention is the one the 04:31 run
invented — `docs/memory-consolidate/replacements/<id>.md` — and it carries the
same caveat: `consolidate.sh decide` does not know about it, so deciding
`57039cfc` orphans this file.

**Also corrected here:** the item's evidence says *19 markers*, measured on the
four files this run was pointed at. The corpus-wide count is **50, across 15
files** (see the run report's Correction section). The bullet below is written
with the true number.

Append as a new bullet at the end of `## Step 5 — Standing checks`, after
*"A store nobody reads"*:

````markdown
- **Drain the `conflicts-with` markers — they are Phase A's queue into this one.**
  Phase A never resolves a contradiction; it appends the new fact and marks the
  old text (`memory-harvest/SKILL.md`: *"That marker is a work item for
  `/memory-harvest`"*). Nothing else reads them, so an undrained marker is a
  decision nobody will ever take. Measured 2026-09-12: **50 live markers across
  15 stores and instruction files**, of which two were covered by an open queue
  item — and both were reached through the store text, not through the marker.

  ```bash
  grep -rn 'conflicts-with:' $(bash "$D" scan | cut -f1) $(bash "$D" instructions | cut -f1)
  ```

  For each, resolve the quoted text to where it still lives — **normalise
  whitespace first**, because a store wraps prose and an exact substring match
  reports a false orphan (measured: 11 of 19 markers in one sample looked
  orphaned under a literal `grep -F`, 2 under a whitespace-normalised match).
  Then:

  - quote resolves inside the corpus -> file one `contradiction` (or `delete`,
    when the new text verifiably supersedes), anchored on the section holding the
    **stale** text, discriminated by its line range.
  - quote resolves only to a file outside `scan`/`instructions` (a `README.md`, a
    spec) -> **not this phase's population**; say so in the report and file
    nothing.
  - quote resolves nowhere near the marker and nothing in the enclosing section
    contradicts it -> the marker itself is the defect; file it so a human can
    delete or move it. This happens: `machines/.claude/memory/project.md:3487`
    quotes the `pull --ff-only` bullet while sitting at the end of an unrelated
    sub-bullet, and Phase A's own `c66abc2` repaired two such misplacements the
    same morning — only the two someone happened to look at.

  A marker already covered by an open item is suppressed like any other
  candidate — **check the queue by TEXT, not only by id**: the marker's anchor is
  the stale text's section, which is rarely the section an existing item names.

  50 markers is more than one run should file at once. Work them **oldest first**
  by their `<!-- src: -->` line, cap the batch, and report the remaining count so
  the backlog is visible rather than silently permanent.
````

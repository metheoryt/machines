# Fleet SSH trust — PUBLIC keys only, one per host. Safe to commit.
#
# THIS FILE IS AUTHORITATIVE ON EVERY PLATFORM. Windows (provision/windows.ps1
# → administrators_authorized_keys) regenerates wholesale; POSIX
# (tier_ssh_trust and ssh-wsl.sh step 4 → ~/.ssh/authorized_keys) rewrites a
# managed span
#     # >>> fleet-trust (managed by machines) >>>  …  # <<< fleet-trust <<<
# and leaves every line outside it untouched. A fleet key already present
# outside the span is absorbed into it on the next run. So adding, removing OR
# RENAMING a line here reaches every member.
#
# ⚠ PROPAGATION IS NOT INSTANT — it lands when a box next provisions. A
# revocation you need to take effect NOW must still be applied by hand on each
# member. (It used to never propagate at all on POSIX: the merge was
# append-only and pruned nothing, so a deleted line was revoked on Windows and
# nowhere else. Fixed 2026-08-01.)
#
# ⚠ A REINSTALLED HOST GETS A NEW KEY AND NOTHING ADDS IT HERE AUTOMATICALLY.
# That is exactly how latitude locked itself out of g16 on 2026-08-01: the
# box was rebuilt as Debian, minted a new me@latitude, and this file still
# listed only its dead NixOS-era key. Update this file in the same change that
# rebuilds a host.
#
# Comments are `<login>@<logical fleet name>` — the fleet key / ssh alias /
# tailnet node — NEVER the OS hostname. Hostnames churn far faster: g16 has
# been DESKTOP-4PQ6V6B, then ME-G614JV, now g614jv; g15 was methe-server, then
# g513ie; latitude still answers `uname -n` with latitude5520, a machine that no
# longer exists.
#
# TWO logical names have changed, both for the same defect — a role word that
# had stopped naming a role:
#   2026-08-27  `server` -> `g15`.  latitude had taken the services role, and
#               `server` is ALSO the linux.sh profile latitude runs, so one word
#               meant two things in one manifest.
#   2026-09-12  `desktop` -> `g16` (and the WSL nickname `g16-wsl` ->
#               `g16-wsl`).  Three of five members are laptops, so `desktop`
#               distinguished nothing. This one is a REVERT: the directory was
#               `hosts/g16/` until `ea0409c` renamed it on 2026-07-20.
# Names are model-based now, `hub` excepted. If you rename another, the tailnet
# node, the dotfiles branch and this file all have to move in the same change.
#
# sshd ignores the comment — every human pruning this file does not.
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAID1phM6MJjQbFKInnKyjVPYrMGs7AxTuq3+Y43V0QzQX me@latitude
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDItNXgaJyDHkiKcLnFSRFbQ1dIk4LeQjlmWBk6sTcwI me@g16-wsl
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIH15XqDE0jcoeF5H2BJP39aBjvSPMLzB0W/n/m0SSNwD methe@g16
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDzgLmdZv1ZYoG5wNujBWADuQlzg6xPweUa78JYW9NcW me@g15
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHC5QVL/9jlSdmswvx9KWZ6rKVT+fr97sr9zAOoFIPKG me@air

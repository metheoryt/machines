---
name: kb-refresh
description: Renamed 2026-09-11. Use cyphy:memory-harvest instead — this stub exists only so a scheduled job still pointing at the old name does not silently do nothing.
---

# kb-refresh was replaced by memory-harvest

**Invoke `cyphy:memory-harvest` now and follow it.** Do not try to do the work from this
file; it contains none of it.

Not a pure rename — it covers every repo on the box, not one, and only proposes changes to shared fleet memory.

This stub is temporary. It stays until every Orca Automation has been repointed,
then it is deleted — say so in your report if you were reached through it, so
the automation gets fixed.

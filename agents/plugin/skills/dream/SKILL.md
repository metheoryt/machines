---
name: dream
description: Renamed 2026-09-11. Use cyphy:memory-consolidate instead — this stub exists only so a scheduled job still pointing at the old name does not silently do nothing.
---

# dream was renamed to memory-consolidate

**Invoke `cyphy:memory-consolidate` now and follow it.** Do not try to do the
work from this file; it contains none of it.

The rename went with `/kb-refresh` → `/repo-harvest`, so the two jobs are named
for their scope: one per repo, one for the whole memory corpus.

This stub is temporary. It stays until every Orca Automation has been repointed,
then it is deleted — say so in your report if you were reached through it, so
the automation gets fixed.
